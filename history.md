# 📜 Mart app 2 (마트 장보기 예산 계산기 2) - 변경 이력 (History)

## 📌 개요
본 문서는 **Mart app 2 (마트 장보기 예산 계산기 2)**의 전체 개발 변경 이력, 기능 추가, 정밀 알고리즘 개선, UI/UX 디자인 업데이트 및 배포 이력을 기록한 종합 변경 문서입니다.

---

## 🏷️ 버전별 변경 이력 (Version History)

### 📜 v1.3.84 (26-08-08) - 모바일 구매내역 모달 스크롤 수정
- **모바일 조회 UX 개선**:
  - 구매내역이 긴 경우 모바일에서 모달 본문이 화면 안에서 세로 스크롤되도록 높이 제한과 내부 스크롤을 추가.
  - 구매내역 조회 필터의 2줄 배치 개선은 유지하면서 긴 목록도 닫기 버튼과 함께 접근 가능하도록 정리.

### 📜 v1.3.83 (26-08-08) - 모바일 구매내역 조회 필터 2줄 배치
- **모바일 조회 UI 개선**:
  - 구매내역 조회 모달의 월 선택 컨트롤과 `조회/연간/전체` 버튼을 분리해 작은 화면에서 2줄로 표시.
  - 모바일 폭에서 각 버튼이 화면 밖으로 밀리지 않도록 동일 폭 그리드로 정리.

### 🧾 v1.3.82 (26-08-08) - 영수증 JSON 변환 자동 저장 규칙 추가
- **영수증 변환 절차 보강**:
  - 사용자가 영수증 JSON 변환을 요청하면 JSON 문법과 합계를 검증한 뒤 통과 시 `영수증_YYYY-MM-DD.json`으로 저장하도록 `receipt.md`에 명시.
  - 검증 실패 시 파일 저장을 하지 않고 오류 사유를 먼저 보고하도록 규칙을 추가.

### 📒 v1.3.81 (26-08-08) - 월별 가계부 기능 추가
- **가계부 조회 기능 추가**:
  - `📒 가계부` 버튼과 모달을 추가해 선택 월 기준 전체/이마트/코스트코 지출을 표시.
  - 이마트 월 예산 `290,000원`, 코스트코 월 예산 `300,000원` 기준으로 잔여/초과 금액을 계산.
  - 지난달 대비 증가/감소, 연간 누적 지출, 마트별 지출 비중, 예산 80% 이상 경고를 표시.
  - `/api/db/household-summary` API를 추가해 구매내역 DB 기준으로 월별/전월/연간 집계를 제공.

### 🧾 v1.3.80 (26-08-08) - 업로드 예제 파일 추가
- **예제 파일 생성**:
  - 영수증 JSON 업로드 예제 `예제_영수증_Costco.json`, `예제_영수증_Emart.json`을 추가.
  - 품목 MD 업로드 예제 `예제_품목_Costco.md`, `예제_품목_Emart.md`를 추가.

### 🏬 v1.3.79 (26-08-08) - 기본 예산 설정 DB 저장
- **기본 설정 저장소 추가**:
  - Neon DB에 `app_settings` 테이블을 추가해 기본 예산 설정값을 저장.
  - `emart_regular_default_budget=60000`, `emart_sunday_default_budget=50000`, `costco_default_budget=300000` 설정을 DB에 upsert.

### 🏬 v1.3.78 (26-08-08) - 일요일 이마트 기본 예산 DB 저장
- **예산 DB 동기화 보강**:
  - 이마트 일요일 기본 예산 5만원을 DB 기본 예산으로도 저장하도록 서버 동기화 로직을 추가.
  - 기존 기본값 6만원으로 저장된 이마트 예산은 일요일에 5만원 기본값으로 보정.
  - 프론트 상태 저장 시 장바구니와 함께 예산도 `/api/db/budget`에 동기화.

### 🏬 v1.3.77 (26-08-08) - 이마트 일요일 기본 예산 조정
- **예산 기본값 개선**:
  - 이마트 기본 예산을 평일/토요일 6만원, 일요일 5만원으로 분리.
  - 사용자가 저장한 예산이 없을 때 일요일 기본값으로 5만원을 적용.

### 🏬 v1.3.76 (26-08-08) - 이마트 화면 표시명 정리
- **마트명 표시 개선**:
  - 내부 키 `Emart`가 전광판에 노출되지 않도록 `이마트 장보기`로 표시.
  - 품목 초기화 버튼도 화면 표시명 `이마트`를 사용하도록 정리.

### 🏬 v1.3.75 (26-08-08) - 마트별 영업시간 전광판 표시
- **영업시간 안내 추가**:
  - 이마트/코스트코 전광판 휴무일 안내에 마트별 영업시간을 함께 표시.
  - 이마트 하남점은 `10:00~23:00`, 코스트코 하남점은 `10:00~22:00` 기준으로 표시.

### 🏬 v1.3.74 (26-08-08) - 휴무일 전광판 점멸 표시
- **휴무일 강조 표시 추가**:
  - 오늘이 둘째·넷째 일요일 휴무일이면 전광판의 휴무일 안내 문구가 점멸되도록 처리.

### 🏬 v1.3.73 (26-08-08) - 휴무일 문구 구분자 정리
- **전광판 휴무일 문구 개선**:
  - 휴무일 안내의 월/일 사이 `/` 표시를 제거해 `8월 9일, 23일 코스트코 휴무일` 형식으로 표시.

### 🏬 v1.3.72 (26-08-08) - 이마트 하남 탭 당일 표시
- **이마트 탭 조건 수정**:
  - 오늘이 해당 월 둘째·넷째 일요일 당일일 때만 이마트 탭을 `이마트 (하남)`으로 표시.
  - 전광판 아래 월별 이마트/코스트코 휴무일 안내는 유지.

### 🏬 v1.3.71 (26-08-08) - 코스트코 휴무일 전광판 및 탭 표시
- **코스트코 휴무 표시 추가**:
  - 코스트코 탭에서도 해당 월 둘째·넷째 일요일 날짜를 계산해 전광판에 `0월/0일, 0일 코스트코 휴무일` 형식으로 표시.
  - 오늘이 둘째·넷째 일요일 당일이면 코스트코 탭을 `코스트코(휴무)`로 표시.

### 🏬 v1.3.70 (26-08-08) - 이마트 월별 휴무일 전광판 표시
- **이마트 휴무일 표시 방식 변경**:
  - 해당 월의 둘째·넷째 일요일 날짜를 계산해 전광판에 `0월/0일, 0일 이마트 휴무일` 형식으로 표시.
  - 이마트 탭 문구는 다시 `이마트`로 고정하고, 코스트코 탭에서는 휴무일 문구를 숨김.

### 🏬 v1.3.69 (26-08-08) - 이마트 하남 휴무 주차 탭 표시
- **이마트 탭 조건부 표시**:
  - 전광판 아래 상시 휴무 안내를 제거.
  - 현재 날짜가 매월 둘째·넷째 일요일이 포함된 주차일 때만 이마트 탭을 `이마트 (하남)`으로 표시.

### 🏬 v1.3.68 (26-08-08) - 이마트 휴무 안내 표시
- **전광판 안내 추가**:
  - 이마트 탭에서 전광판 아래에 `매월 둘째·넷째 일요일 이마트 휴무` 안내를 작은 글씨로 표시.
  - 코스트코 탭에서는 휴무 안내를 숨기도록 처리.

### 🧭 v1.3.67 (26-08-08) - 구매내역 월 표시 폭 축소
- **구매내역 조회 UI 정리**:
  - 상단 월 표시 폭을 줄이고 버튼 간격을 축소해 한 줄 배치 여유를 확보.

### 🧭 v1.3.66 (26-08-08) - 구매내역 상단 필터 한 줄 압축
- **구매내역 조회 UI 정리**:
  - 월 이동 버튼, 월 표시, `조회/연간/전체` 버튼 폭을 줄여 상단 필터가 한 줄에 들어가도록 조정.

### 🧭 v1.3.65 (26-08-08) - 구매내역 월 선택 달력 제거
- **구매내역 조회 UI 정리**:
  - 월 선택 입력을 읽기 전용 텍스트로 바꿔 달력 팝업을 제거.
  - `▼/▲` 버튼으로만 월을 조정하도록 정리하고, `전체` 버튼이 윗줄에 유지되도록 월 표시 폭을 축소.

### 🧭 v1.3.64 (26-08-08) - 조회/연간/전체 버튼 월 선택 줄 배치
- **구매내역 조회 UI 정리**:
  - `조회/연간/전체` 버튼을 월 선택 줄에 함께 배치.
  - 두 번째 줄에는 `월간 랭킹/연간 랭킹/저장` 버튼만 남기도록 정리.

### 🧭 v1.3.63 (26-08-08) - 구매내역 조회 버튼 2줄 정렬
- **구매내역 조회 UI 정리**:
  - 필터 버튼을 1줄 `조회/연간/전체`, 2줄 `월간 랭킹/연간 랭킹/저장`으로 정렬.
  - 월간/연간 랭킹 버튼에 `🏆` 아이콘을 표시.

### 💾 v1.3.62 (26-08-08) - 구매내역 저장 버튼과 랭킹 범위 분리
- **구매내역 조회 기능 개선**:
  - 장바구니 영역의 `로컬 파일로 저장 (MD 저장)` 버튼 제거.
  - 구매내역 조회 모달 필터 영역에 `💾 저장` 버튼을 추가해 현재 조회 결과를 Markdown으로 저장.
  - 랭킹 버튼을 `월간 랭킹`, `연간 랭킹`으로 분리.

### 🧭 v1.3.61 (26-08-08) - 로컬 파일 저장 버튼 위치 조정
- **장바구니 버튼 배치 개선**:
  - `로컬 파일로 저장 (MD 저장)` 버튼을 조회/업로드/다운로드 2x2 버튼 아래, 장바구니 액션 영역 맨 아래로 이동.

### 🏆 v1.3.60 (26-08-08) - 월별 품목 소비 랭킹 추가
- **구매내역 조회 기능 추가**:
  - 구매내역 조회 필터 영역에 `🏆 랭킹` 버튼 추가.
  - 선택 월의 현재 마트 품목 TOP 10을 수량순/금액순으로 전환해 표시.
  - 필터 버튼이 늘어나도 줄바꿈으로 안정적으로 배치되도록 정리.

### 🧾 v1.3.59 (26-08-08) - 영수증 업로드 미리보기 추가
- **영수증 업로드 안전장치**:
  - 영수증 JSON 업로드 후 바로 저장하지 않고 미리보기 모달을 먼저 표시.
  - 품목, 개수, 총합을 확인한 뒤 `저장`을 눌러야 DB에 구매내역으로 저장되도록 변경.

### 🔥 v1.3.58 (26-08-08) - 구매내역 특이 가격 표시 보강
- **구매내역 조회 UI 개선**:
  - 구매내역 카드의 코스트코 품목도 가격 끝자리가 70원 또는 00원이면 `🔥` 표시.
  - 전체 조회에서 현재 탭이 아닌 각 구매내역의 마트 기준으로 특이 가격을 판단하도록 수정.

### 📋 v1.3.57 (26-08-08) - 구매내역 품목 가나다순 표시
- **구매내역 조회 UI 개선**:
  - 구매내역 카드 안의 품목 목록을 품목명 기준 가나다순으로 정렬해 표시.

### 🗑️ v1.3.56 (26-08-08) - 구매내역 삭제 확인 절차 보강
- **구매내역 삭제 안전장치**:
  - 삭제 확인창에 구매일시와 금액을 표시.
  - 사용자가 삭제를 취소하면 취소 안내를 표시하고 삭제 요청을 보내지 않도록 정리.

### 📋 v1.3.55 (26-08-08) - 구매내역 품목 3열 표시
- **구매내역 조회 UI 개선**:
  - 구매내역 카드의 품목 줄을 `품목 / 개수 / 총합` 3열로 정렬.
  - 기존 `품목 x 수량` 표기를 `품목  수량개  총합` 형태로 변경.

### 📝 v1.3.54 (26-08-08) - 영수증 품명 표준화 가이드 보강
- **영수증 변환 가이드 보강**:
  - 공백, 하이픈, 용량 표기 위치만 다른 품목은 대표 품명으로 표준화하도록 `receipt.md`에 명시.
  - `쇠고기육포280G`를 `쇠고기육포 280G`로 저장하도록 중복 정리 이력을 보강.
  - DB 저장 전 비교 키 규칙을 `품명_중복정리_이력.md`에 추가.

### 📝 v1.3.53 (26-08-08) - 구매내역 조회 문구 정리
- **UI 문구 변경**:
  - 버튼명 `저장내역조회`를 `구매내역 조회`로 변경.
  - 버튼명 `계산결과 저장 (DB 저장)`을 `장바구니 저장`으로 변경.
  - 구매내역 모달 제목과 로딩/삭제 안내 문구를 `구매내역` 기준으로 정리.

### 🦐 v1.3.52 (26-08-08) - KS새우 품명 정규화
- **품목명 중복 정리 보강**:
  - `KS새우...` 입력을 규격별 `새우 ...` 대표 품명으로 정규화.
  - 영수증 변환, 품목 파일 업로드, 서버 품목 저장 경로의 새우 품명 규칙을 함께 보강.

### 📅 v1.3.51 (26-08-08) - 저장내역 마트 정보 표시
- **저장내역 조회 UI 개선**:
  - 저장내역 카드에 구매한 마트 배지를 표시.
  - DB 키 `Costco`, `Emart`를 화면에서는 `코스트코`, `이마트`로 표시하도록 정리.

### 📅 v1.3.50 (26-08-08) - 저장내역 모든 조회 모드 총합 표시
- **저장내역 조회 개선**:
  - `조회`, `연간`, `전체` 모드 모두에 구매 총합 배너를 표시.
  - 모드별로 `월 구매 총합`, `연간 구매 총합`, `전체 마트 월 구매 총합` 문구를 구분.

### 📅 v1.3.49 (26-08-08) - 저장내역 조회 버튼 현재월 고정
- **저장내역 조회 필터 개선**:
  - `조회` 버튼을 누르면 선택 입력값과 관계없이 현재 월의 현재 마트 구매내역을 조회하도록 변경.

### 📅 v1.3.48 (26-08-08) - 연간 조회 캡션 정리
- **저장내역 조회 UI 개선**:
  - 연간 조회 시 캡션에서 불필요한 `YYYY-MM ·` 월 표시를 제거.

### 📅 v1.3.47 (26-08-08) - 연간 구매 총합 표시
- **저장내역 조회 개선**:
  - `연간` 조회 시 선택 연도/현재 마트 기준 구매 총합을 상단 배너로 표시.

### 📅 v1.3.46 (26-08-08) - 저장내역 연간 조회 버튼 추가
- **저장내역 조회 필터 개선**:
  - `조회`는 선택한 월의 현재 마트 구매내역만 조회.
  - `연간`은 선택한 연도의 현재 마트 전체 구매내역 조회.
  - `전체`은 선택한 월의 전체 마트 구매내역 조회.

### 📅 v1.3.45 (26-08-08) - 저장내역 조회/전체 버튼 동작 구분
- **저장내역 조회 필터 개선**:
  - `조회` 버튼은 선택한 월의 현재 마트 구매내역만 조회하도록 추가.
  - `전체` 버튼은 선택한 월의 전체 마트 구매내역을 조회하도록 변경.
  - 현재 조회 범위를 월/마트 기준으로 확인할 수 있는 보조 문구를 추가.

### 📅 v1.3.44 (26-08-08) - 저장내역 현재월 기본 조회와 월 이동 버튼 추가
- **저장내역 조회 UI 개선**:
  - 저장내역 조회를 열면 현재 년월이 기본 선택되도록 변경.
  - 월 선택 입력 옆에 ▲/▼ 버튼을 추가해 다음월/이전월로 바로 이동 조회할 수 있도록 개선.

### 📅 v1.3.43 (26-08-08) - 저장내역 월별 접기 화면 추가
- **저장내역 조회 UI 개선**:
  - 구매내역을 월별 접기/펼치기 섹션으로 렌더링하도록 변경.
  - 전체 조회 시 최신 월만 기본 펼침 상태로 표시하고, 특정 월 조회 시 해당 월을 펼쳐서 표시.

### 🧾 v1.3.42 (26-08-08) - 영수증 DB 저장 안전 가이드 보강
- **영수증 변환 가이드 보강**:
  - `receipt.md`에 DB 저장 전 필수 검증 규칙과 품목명 정제 순서를 추가.
  - 쿠폰 할인 차감 방식에 맞춰 예시 출력에서 별도 음수 쿠폰 품목을 제거.
  - `품명_중복정리_이력.md`에 DB 저장 품목명 금지 문자와 쿠폰 처리 규칙을 추가.
  - 영수증 JSON 업로드 시 `items` 계산 합계와 `totalAmount`가 다르면 저장을 중단하도록 보강.

### 📅 v1.3.41 (26-08-08) - 기존 한글 마트 키 저장내역 조회 보정
- **저장내역 조회 보강**:
  - 기존 구매내역에 남아 있던 한글 `코스트코`, `이마트` 마트 키를 표준 키 `Costco`, `Emart`로 정리.
  - 월별 저장내역 조회 시 표준 키와 기존 한글 키를 함께 조회해 과거 내역 누락을 방지.

### 📅 v1.3.40 (26-08-08) - 코스트코 월별 저장내역 조회 보정
- **저장내역 조회 오류 수정**:
  - 화면 탭 값 `코스트코`와 DB 저장 키 `Costco`가 달라 6월 내역이 보이지 않던 문제를 수정.
  - 구매내역 조회 API에서 한글/영문 마트명을 표준 DB 키로 정규화하도록 보강.

### 📅 v1.3.39 (26-08-08) - 저장내역 월별 조회 및 버튼 배열 정리
- **저장내역 조회 개선**:
  - 구매내역 조회 API에 `month=YYYY-MM` 필터를 추가.
  - 저장내역 조회 모달 상단에 월 선택 필터와 전체 조회 버튼을 추가.
- **하단 버튼 레이아웃 정리**:
  - 등록 품목/가격 조회, 품목 파일 업로드, 영수증 JSON 업로드, 품목/가격 정보 다운로드 버튼을 2 x 2 배열로 정렬.

### 🧾 v1.3.38 (26-08-08) - 영수증 할인 차감 정제 규칙 반영
- **영수증 JSON 정제 규칙 보강**:
  - 쿠폰/할인 행을 별도 음수 품목으로 저장하지 않고 대상 품목 금액에서 차감하도록 `receipt.md`를 수정.
  - 동일 품목은 수량과 최종 금액을 합산하고, 품목명에서 `CPN`, `IRC`, 특수기호, 불필요한 큰따옴표와 쉼표를 제거하도록 가이드.
  - `영수증_2026-06-05.json`을 할인 차감 후 최종 단가 기준으로 재생성.

### 🧾 v1.3.37 (26-08-08) - 코스트코 영수증 저장 마트 키 보정
- **구매내역 저장 보정**:
  - 영수증 JSON의 `martName`이 `코스트코`일 때 DB `purchase_records.mart_id`를 표준 키 `Costco`로 저장하도록 서버 매핑을 수정.
  - 코스트코/이마트 영수증 업로드 후 마트별 구매내역 조회에서 누락되지 않도록 보강.

### 🧾 v1.3.36 (26-08-08) - 마트별 영수증 품명 중복 정리 가이드 추가
- **영수증 변환 가이드 보강**:
  - 변환 결과 JSON에 `martName`을 필수로 넣도록 `receipt.md` 규칙을 명확히 정리.
  - `품명_중복정리_이력.md`를 추가해 코스트코/이마트별 대표 품명과 중복 후보 이력을 분리.
  - 마트명이 없으면 변환하지 말고 `코스트코` 또는 `이마트` 중 하나를 먼저 입력받도록 가이드.
  - 영수증 JSON 업로드 시 `martName`이 없거나 허용 값이 아니면 저장을 막아 마트 구분 오류를 방지.

### 🧾 v1.3.35 (26-08-08) - 영수증 구매일시 필수 가이드 보강
- **영수증 변환 규칙 보강**:
  - `receipt.md`에 구매일시 필수 규칙을 명확히 추가.
  - 영수증 원문이나 요청에 구매일시가 없으면 JSON 변환을 진행하지 말고 사용자에게 `YYYY-MM-DD HH:mm:ss` 형식으로 입력받도록 가이드.

### 🧾 v1.3.34 (26-08-08) - 영수증 JSON 업로드 기능 추가
- **영수증 구매내역 저장**:
  - 화면 하단에 `영수증 JSON 업로드` 버튼을 추가.
  - `receipt.md` 규칙으로 만든 JSON을 업로드하면 `timeStr`, `items`, `totalAmount`를 구매내역 DB와 로컬 `구매내역.md`에 저장하도록 구현.

### 🧾 v1.3.33 (26-08-08) - 영수증 JSON 저장 파일명 가이드 추가
- **영수증 변환 결과 저장 규칙**:
  - `receipt.md`에 변환 결과를 `영수증_YYYY-MM-DD.json` 파일명으로 저장하도록 규칙을 추가.
  - 출력 JSON에 `fileName` 필드를 포함하도록 예시를 보강.

### 🧾 v1.3.32 (26-08-08) - 영수증 구매내역 변환 가이드 추가
- **영수증 변환 프롬프트 문서화**:
  - 코스트코 영수증 원문을 구매내역 DB 저장용 JSON으로 변환하는 `receipt.md` 가이드를 추가.
  - 쿠폰 할인, 대표 품명 정리, 총액 검증, `items` 배열 출력 규칙을 포함.

### 📊 v1.3.31 (26-08-08) - 가격 이력 통계 다운로드 추가
- **품목/가격 내보내기 통계 보강**:
  - CSV/Markdown 다운로드에서 `변동 이력 수` 옆에 `최저`, `최고`, `평균` 컬럼을 추가.
  - JSON 다운로드에는 각 품목별 `priceStats` 객체로 이력 수, 최저가, 최고가, 평균가를 포함.

### 📥 v1.3.30 (26-08-08) - 품목/가격 다운로드 헤더 오류 수정
- **다운로드 파일명 보정**:
  - `GET /api/db/export` 응답의 `Content-Disposition` 파일명을 ASCII 안전 이름으로 변경.
  - `코스트코` 등 한글 마트명이 헤더에 직접 들어가 발생하던 `Invalid character in header content` 오류를 수정.

### 🧭 v1.3.29 (26-08-08) - 업로드 MD 생성 대표 품명 적용
- **품목 업로드 중복 방지 적용**:
  - 품목 파일 업로드 시 대표 품명 규칙을 적용해 동일 품목이 여러 이름으로 추가되지 않도록 보강.
  - 서버가 업로드 결과를 `.md` 파일로 다시 생성할 때도 대표 품명으로 정리된 목록을 저장하도록 수정.

### 🧭 v1.3.28 (26-08-08) - 품목 등록/업로드 중복 검토 가이드 추가
- **대표 품명 업로드 가이드**:
  - 품목 신규 등록 또는 파일 업로드 전 기존 품목과 유사/중복 여부를 검토하도록 `dev.md` 절차에 명시.
  - 동일 품목은 새 이름으로 추가하지 않고 기존 대표 품명으로 단가만 갱신하도록 정리.

### 🧩 v1.3.27 (26-08-08) - 기린캔 유사 품목 병합
- **코스트코 기린캔 병합**:
  - `기린캔 8개`를 제거하고 대표명 `기린캔 500ML X 8`로 통합.
  - 단가는 최신 요청 기준 `11,990원`으로 유지하고 Neon DB를 가격표 기준으로 다시 동기화.

### 🧩 v1.3.26 (26-08-08) - 코스트코 유사 품목 병합
- **소노마 샤르도네 병합**:
  - `와인 소노마 샤르도네`와 `소노마 샤도네이`를 대표명 `소노마 샤르도네`로 합침.
  - 코스트코 기본 품목 목록도 현재 가격표 기준으로 정리해 새 환경에서도 중복이 재생성되지 않도록 보강.

### 🔥 v1.3.25 (26-08-08) - 코스트코 특가 표시 아이콘 변경
- **코스트코 가격표 아이콘 개선**:
  - 코스트코 `70원/00원` 조건 표시를 `*`에서 `🔥` 아이콘으로 변경.
  - 아이콘은 기존과 동일하게 UI 표시 전용으로 유지.

### ✳️ v1.3.24 (26-08-08) - 코스트코 별표 표시 즉시 갱신
- **가격 변경 후 표시 동기화**:
  - 등록 품목 모달에서 코스트코 단가를 수정했을 때 별표 조건이 바뀌면 빠른 선택칩과 자동완성 목록도 즉시 갱신되도록 보강.
  - 새 품목 입력 및 가격 변경 모두 `70원/00원` 조건에 맞춰 화면 표시 품목명 앞 `*`가 반영되도록 정리.

### ✳️ v1.3.23 (26-08-08) - 코스트코 특가 표시 별표 추가
- **코스트코 가격표 표시 규칙**:
  - 코스트코 품목 단가 끝 두 자리가 `70` 또는 `00`이면 화면 표시 품목명 앞에 `*`를 붙이도록 추가.
  - `*`는 UI 표시 전용으로 적용해 실제 저장 품목명과 DB 품목명은 변경하지 않음.

### 🧾 v1.3.22 (26-08-08) - 코스트코 단가 파싱 오류 보정
- **코스트코 품목 재등록 보정**:
  - 천 단위 쉼표가 있는 단가는 첫 쉼표 뒤 전체를 가격 문자열로 읽어야 함을 확인하고 서버/프론트 품목 파서를 해당 규칙으로 유지.
  - `청화 페페론치노` 단가를 `15,900원`으로 정정하고 코스트코 DB 품목을 파일 기준으로 다시 등록.

### 🛒 v1.3.21 (26-08-08) - 코스트코 품목 단가 반영
- **코스트코 단가 업데이트**:
  - 요청받은 코스트코 14개 품목 단가를 `품목_코스트코.md`에 반영.
  - 기존 표기와 다른 품목명은 요청 기준으로 정규화하고, 신규 품목은 목록에 추가.

### 🏷️ v1.3.20 (26-08-08) - 버전 날짜 두 자리 연도 표기
- **버전 배지 표기 축약**:
  - 전광판 버전 날짜를 `2026-08-08`에서 `26-08-08` 형식으로 변경.
  - `APP_VERSION`, HTML 배지, 패키지 버전, 개발 절차 문서의 버전 날짜 규칙을 함께 동기화.

### 🟩 v1.3.19 (2026-08-08) - Neon DB 로고 배경 투명화
- **로고 표시 개선**:
  - `public/assets/neon-db-logo.png`의 검은 배경 픽셀을 투명 처리.
  - 전광판 오른쪽 아래 배지의 배경과 테두리를 제거해 Neon DB 로고만 보이도록 조정.

### 🟩 v1.3.18 (2026-08-08) - Neon DB 로고 단독 표시
- **전광판 저장소 배지 정리**:
  - 오른쪽 아래 저장소 배지에서 `Postgres` 텍스트를 제거하고 Neon DB 로고만 남김.
  - 로고 단독 표시에 맞춰 배지 패딩과 크기를 조정.

### 🟩 v1.3.17 (2026-08-08) - 실제 Neon DB 로고 이미지 적용
- **전광판 Neon 로고 반영**:
  - 첨부된 Neon DB 로고 PNG를 `public/assets/neon-db-logo.png`로 추가.
  - 전광판 오른쪽 아래 Neon Postgres 배지의 임시 `N` 텍스트 마크를 실제 Neon 로고 이미지로 교체.

### 🛠️ v1.3.16 (2026-08-08) - 로컬 Neon Postgres 환경 변수 로드 보강
- **로컬 개발 실행 보정**:
  - `npm run dev` 실행 시 `server.js`가 `.env.local` 또는 `.env`를 먼저 읽어 `DATABASE_URL`을 `process.env`에 주입하도록 보강.
  - 로컬 환경에서 `DATABASE_URL environment variable is required for Neon Postgres.` 오류가 발생하지 않도록 수정.

### 🟢 v1.3.15 (2026-08-08) - Neon Postgres 로고형 배지 적용
- **전광판 저장소 로고 개선**:
  - 전광판 오른쪽 아래 저장소 표시를 단순 `NeonDB` 텍스트에서 Neon Postgres 로고형 배지로 변경.
  - 초록색 `N` 마크와 `Postgres` 텍스트를 함께 표시해 Neon Postgres 연결 상태를 더 직관적으로 보이도록 개선.

### 📝 v1.3.14 (2026-08-08) - 개발 이력 및 절차 문서 동기화
- **개발 문서 최신화**:
  - `dev.md`의 GitHub 배포 저장소를 `mart2.git` 기준으로 정정하고 배포 전 검증 절차를 명시.
  - Neon Postgres/NeonDB 저장 기준, 전광판 `NeonDB` 배지 위치, 초성 검색 보강 규칙을 개발 절차에 반영.
  - 최신 기준 버전을 `v1.3.14 (2026-08-08)`로 정리하고 변경 이력 작성 표준에 배포 커밋 기록 원칙을 추가.

### 🗄️ v1.3.13 (2026-08-08) - NeonDB 표시 배지 추가
- **전광판 저장소 표시**:
  - 전광판 오른쪽 아래에 `NeonDB` 배지를 추가해 현재 저장소 전환 상태를 화면에서 바로 확인할 수 있도록 개선.
  - 기존 버전 배지와 같은 글래스 스타일로 표시해 전광판 UI와 자연스럽게 어울리도록 조정.

### 🔤 v1.3.12 (2026-08-08) - 단어 경계 초성 검색 보강
- **품목 초성 검색 개선**:
  - `방울 토마토`처럼 앞 단어가 붙은 품목도 뒤 단어의 연속 초성(`ㅌㅁㅌ`)으로 검색되도록 자동완성 매칭 로직을 보강.
  - 공백 없이 저장된 `방울토마토` 형태도 2자 이상 연속 초성 검색으로 찾을 수 있도록 보완.
  - 기존 전체 품목명 시작 초성 및 단어 시작 초성 검색 동작은 유지.

### 📜 v1.3.11 (2026-08-07) - Neon Postgres 저장소 전환
- **Vercel 서버리스 DB 호환성 개선**:
  - `better-sqlite3` 파일 DB 의존성을 제거하고 `pg` 기반 Neon Postgres 연결로 전환.
  - 품목, 가격 이력, 장바구니, 예산, 구매 저장 내역 테이블을 Postgres에서 자동 생성하도록 `db.js`를 재작성.
  - 서버 API를 비동기 DB 호출 방식으로 변경하고, Vercel 환경에서는 로컬 Markdown 백업 파일 쓰기를 건너뛰도록 보강.
  - Postgres 날짜/JSON 반환값을 기존 프론트엔드가 읽을 수 있는 형태로 정규화.

### 📜 v1.3.10 (2026-08-06) - 등록 품목 가격 이력 버튼 열림 보강
- **가격 이력 버튼 이벤트 충돌 방지**:
  - 단가 입력칸의 blur/change 저장 이벤트보다 먼저 동작하는 `pointerdown`에서 가격 이력 모달을 열도록 보강.
  - 이력 모달 표시 시 display/visibility/pointer-events를 명시해 숨김 상태 잔존 가능성을 제거.
  - 이력 버튼 클릭 영역을 키워 터치/클릭 인식 안정성을 개선.

### ✅ v1.3.9 (2026-08-06) - 단가 입력 저장 트리거 보강
- **등록 품목 단가 저장 확정 동작 보강**:
  - 단가 입력칸의 `change` 이벤트에서도 저장을 실행해 입력값 확정 시 DB 저장이 더 안정적으로 일어나도록 수정.
  - 저장 중 입력칸 비활성화 및 저장 중 시각 상태를 추가해 저장 요청 진행 여부를 확인하기 쉽게 개선.

### 💸 v1.3.8 (2026-08-06) - 등록 품목 단가 직접 입력 저장 안정화
- **단가 직접 입력 저장 흐름 개선**:
  - 단가 입력 저장 중 전체 화면 `render()`를 호출하지 않고 해당 입력칸과 저장 품목 상태만 갱신하도록 수정.
  - 서버 저장 요청 중 입력칸을 잠시 비활성화하고, 저장 실패 시 이전 단가로 되돌리도록 보강.
  - 저장 성공 후 입력값을 천 단위 콤마 형식으로 즉시 정리.

### 📌 v1.3.7 (2026-08-06) - 가격 이력 모달 최상단 표시 강제
- **등록 품목 조회 화면 위 가격 이력 모달 표시 보강**:
  - 가격 이력 모달을 열 때 DOM 맨 뒤로 이동시켜 다른 모달보다 나중에 그려지도록 수정.
  - `modal-topmost` 레이어와 최상위 `z-index`를 적용해 등록 품목 조회 화면 위에 확실히 표시되도록 보강.

### 🧾 v1.3.6 (2026-08-06) - 등록 품목 단가 직접 입력 및 가격 이력 표시 개선
- **등록 품목 가격 이력 표시 보강**:
  - 가격 이력 모달을 최상단 레이어로 띄워 등록 품목 조회 화면 위에서도 확실히 보이도록 수정.
- **단가 직접 입력 전환**:
  - 별도 입력 팝업을 제거하고 등록 품목 목록의 가격 영역에서 바로 단가를 입력하도록 변경.
  - Enter 또는 입력칸 포커스 이동 시 단가를 저장하고 DB와 동기화하도록 개선.

### 🏷️ v1.3.5 (2026-08-06) - 전광판 버전/날짜 배지 위치 수정
- **전광판 버전/날짜 정보 우측 상단 배치**:
  - `#app-version-badge`의 실제 HTML 클래스(`.version-badge`)에 위치 스타일이 적용되도록 CSS 선택자를 보강.
  - 전광판 오른쪽 위에서 줄바꿈 없이 표시되도록 정리.

### 📜 v1.3.4 (2026-08-06) - 등록 품목 가격 이력 열림 오류 수정
- **등록 품목 & 단가 목록 모달 가격 이력 복구**:
  - 가격 이력 버튼 클릭 시 목록 모달이 이력 모달을 덮지 않도록 목록 모달을 숨긴 뒤 이력 모달을 표시.
- **가격등록 버튼 간소화**:
  - 가격등록 버튼은 텍스트를 제거하고 연필 아이콘만 남기도록 정리.

### 💾 v1.3.3 (2026-08-06) - 계산결과 저장 피드백 및 DB 실패 처리 개선
- **계산결과 저장 동작 보강**:
  - 저장 요청 중 버튼을 잠시 비활성화하여 중복 클릭을 방지.
  - 서버 응답 실패 메시지를 사용자에게 직접 표시하도록 개선.
  - 저장 내역 창이 열린 상태에서 저장 성공 시 목록을 즉시 새로고침하도록 수정.
- **DB 저장 실패 처리 강화**:
  - 구매 내역 DB 저장 실패를 조용히 무시하지 않고 서버 오류로 반환하도록 수정.

### 🧩 v1.3.2 (2026-08-06) - 장바구니 DB 저장 내역 창 닫기 오류 수정
- **저장 내역 모달 닫기 동작 복구**:
  - `#btn-records-modal-close` 클릭 이벤트를 연결하여 X 버튼으로 창이 닫히도록 수정.
  - 모달 바깥 배경 클릭 및 `Esc` 키 입력 시에도 저장 내역 창이 닫히도록 보강.

### 🛠️ v1.3.1 (2026-08-06) - 등록 품목 삭제 API 오류 수정
- **등록 품목 삭제 API (`DELETE /api/db/item`) 복구**:
  - 프론트의 등록 품목 삭제 버튼이 호출하는 서버 엔드포인트를 추가하여 404 오류를 해결.
  - 삭제 후 SQLite DB와 `품목_<마트>.md` 백업 파일을 함께 동기화하도록 보강.

### 🗄️ v1.3.0 (2026-08-06) - 장바구니 DB 저장 내역 조회, 개별 내역 삭제 및 월별 지출 합계 기능
- **DB 저장 내역 조회 팝업 모달 (`#purchase-records-modal`)**:
  - `💾 계산결과 저장 (DB 저장)` 버튼 우측에 `📜 저장내역조회` 서브 버튼을 추가하여 과거 저장한 장바구니 결제 내역을 언제든 조회 가능.
  - 내역 상단에 **월별 지출 합계(📅 YYYY년 MM월 총 지출)**를 계산하여 파란색 요약 박스로 맨 위에 한눈에 표출.
  - 각 저장 내역 카드의 시간 형식을 `YYYY-MM-DD   HH:mm` (초 제외, 공백 2칸)으로 정돈하고 최종 금액을 붉은색(`red`)으로 강조.
- **개별 저장 내역 삭제 기능 (`DELETE /api/db/records/:id`)**:
  - 저장된 개별 내역 카드 우측 상단에 삭제(`🗑️`) 버튼을 제공하여 불필요한 장바구니 저장 내역을 개별적으로 선택 삭제 지원.

### 🗄️ v1.2.0 (2026-08-06) - SQLite DB 동기화, 품목별 가격 변동 이력 모달 및 다중 포맷 내보내기/다운로드 기능
- **SQLite 3 DB 기반 실시간 동기화**:
  - `better-sqlite3` 도입으로 `mart.db`에 품목, 단가, 가격 변동 이력, 공유 장바구니, 예산 정보를 통합 저장.
  - 다른 기기 및 브라우저에서 접속하더라도 최신 데이터 동기화 지원 (`GET /api/db/sync`, `POST /api/db/item`, `POST /api/db/cart`).
- **품목별 가격 변동 이력 확인 모달 (`#price-history-modal`)**:
  - 장바구니 품목 타임라인에 `📜 이력` 버튼 추가 및 클릭 시 최고가/최저가/최근 단가 요약 및 날짜별 가격 기록 리스트 표출.
- **품목 및 가격 정보 다운로드 (CSV / JSON / MD)**:
  - `GET /api/db/export` API 구축 및 화면 하단 드롭다운 다운로드 메뉴 추가 (`CSV 포맷`, `JSON 포맷`, `Markdown 포맷`).

### 🏷️ v1.1.0 (2026-08-06) - 앱 이름 변경 및 버전 체계 1.1.0 재설정
- **앱 명칭 변경**: 서비스 공식 명칭을 `Mart app 2`로 변경 (`index.html` title, `package.json`, `README.md`, `dev.md` 반영).
- **버전 체계 재설정**: 버전을 `v1.1.0 (2026-08-06)`으로 새 출발 반영 (`package.json`, `public/app.js`, `index.html` 전광판 배지 동기화).

### 💡 v4.5 (2026-08-06) - 옵션 B 자주 구매한 품목(구매 빈도) 우선 스마트 추천
- **스마트 품목 추천 알고리즘 정밀 개선 (옵션 B)**:
  - 추천 후보를 아직 장바구니에 담기지 않은 미담김 품목 중 **`단가 <= 남은 예산` 조건에 부합하는 품목으로 엄격 제한**.
  - 후보 정렬 1순위를 **구매 빈도(`useCount` 내림차순 - 자주 구매한 순)**로 설정하여 남은 예산 범위 안에서 가장 자주 찾는 실용적 품목을 우선 제안.
  - 남은 예산 이하인 추천 품목이 없을 경우 예산 초과 품목을 노출하지 않고 추천 카드를 깔끔히 숨김 처리.
- **개발 명세 동기화**: `dev.md` 섹션 28 명세 업데이트 반영.

### 🎨 v4.4 (2026-08-06) - 자동완성 드롭다운 품목 입력란 바로 아래 배치
- **자동완성 레이어 위치 구조 개선**:
  - 초성/텍스트 자동완성 추천 드롭다운(`.autocomplete-list`)이 가격 입력 상자(`#item-price-input`)와 시각적으로 겹치던 문제 해결.
  - `.autocomplete-container`를 품목 입력란(`#item-name-input`) 전용으로 격리하여 드롭다운이 품목 입력란 바로 아래(`top: 100%`)에 정밀하게 표출되도록 레이아웃 수정.
- **개발 명세 동기화**: `dev.md` 섹션 35 명세 반영.

### 🚀 v4.3 (2026-08-06) - 장바구니 품목 최신순 상단 배치
- **최신 담은 품목 최상단(`index 0`) 배치**:
  - 장바구니에 품목을 추가할 때(폼 입력 담기 및 스마트 추천 담기), 신규 품목이 장바구니 목록 **맨 위(`cart.unshift`)에 즉시 배치**되도록 변경.
  - 새로 담은 품목을 스크롤 이동 없이 화면 상단에서 한눈에 확인하고 바로 수량 조절 및 조작할 수 있도록 UX 개선.
- **개발 명세 동기화**: `dev.md` 섹션 34 명세 반영.

### 🚀 v4.2 (2026-08-05) - 단일 매칭 엔터 가격 검토 이동 & 변경 이력 관리 정책 수립
- **단일 매칭 엔터 입력 가격 검토 포커스 이동**:
  - 품목 입력란에서 초성이나 텍스트 검색 시 매칭 품목이 단 1개일 때 엔터(`Enter`) 키를 누르면 품목명과 저장된 최근 단가가 자동으로 입력 상자에 채워짐.
  - 별도 마우스 클릭 없이 즉시 가격 입력 상자(`itemPriceInput.focus()`)로 포커스가 이동하여 사용자가 가격을 한눈에 검토/수정 후 편하게 담을 수 있도록 편의성 대폭 향상.
- **변경 이력 관리 정책 원칙 수립 (`AGENTS.md` & `dev.md`)**:
  - 코드 수정 및 신규 기능 추가 시마다 `history.md` 변경 이력을 의무적으로 동기화 갱신하도록 `AGENTS.md`(섹션 3) 및 `dev.md`(섹션 33)에 개발 지침 반영.

### ⚡ v4.1 (2026-08-05) - 단일 매칭 품목 자동 가공 엔진
- **단일 매칭 품목 추적 및 자동 세팅**:
  - `currentMatches.length === 1` 상태의 자동완성 결과 추적 엔진 구축.
- **다중 매칭 오작동 방지**:
  - 검색 결과가 여러 개일 경우 무분별한 자동 담기를 방지하여 오입력 차단.

### 🔤 v4.0 (2026-08-05) - 초성 경계 매칭 & 단자음/쌍자음 호환 검색
- **음절 받침(종성) 오매칭 원천 차단**:
  - `ㄱ` 입력 시 `맥주`(`ㅁ` 시작, '맥'의 받침이 `ㄱ`)와 같은 받침 오매칭 항목 완전 제외.
  - 품목명 및 단어의 시작 위치 **'초성(Chosung) 경계 매칭'**만 엄격히 검출.
- **단자음 ↔ 쌍자음 호환 검색 정규화**:
  - `normalizeTenseConsonants` 정규화 엔진 적용.
  - `ㄱ` 입력 시 ➡️ `그린키위` (`ㄱ`), `깐대파` (`ㄲ`) 동시 호환 탐색.
  - `ㄷ` ↔ `ㄸ`, `ㅂ` ↔ `ㅃ`, `ㅅ` ↔ `ㅆ`, `ㅈ` ↔ `ㅉ` 완벽 호환.

### 💰 v3.8 ~ v3.9 (2026-08-05) - 낮은 가격순 칩 정렬 & 예산 초과 폴백 추천
- **등록 품목 칩 낮은 가격순 정렬**:
  - Quick Select Chips 목록을 단가 오름차순(낮은 가격순: `1,480원` ➡️ `1,874원` ...)으로 정렬.
  - 동일 가격대 항목은 구매 빈도(`useCount` 내림차순) 및 가나다순으로 배치.
- **스마트 품목 폴백 추천 엔진**:
  - 남은 예산 이하 품목이 없고 모두 예산을 초과하는 항목만 남아있을 때, 미담김 품목 중 **구매 빈도가 가장 높은 품목(`useCount` 내림차순)**을 탐색하여 맞춤 안내 표출.

### 📐 v3.4 ~ v3.7 (2026-08-05) - 전광판 간섭 방지 레이아웃 & 코스트코 정결화
- **전광판 간섭 방지 오프셋 정렬**:
  - `💡 스마트 추천` 버튼과 예산 상태 표시 영역(`.status-display`) 간 시각적 겹침 방지를 위해 예산 영역 우측 36px 오프셋 이동.
- **코스트코 품목 24종 단가 100% 검증**:
  - 가격 미입력 미지정 항목(`돼지고기`, `소고기`, `크로와상`, `베이글`, `우유 2입`, `기린캔 500ML`) 전량 제거.
  - 단가가 명확히 기재된 유효 24종 품목 데이터만 동기화.

### 🔤 v2.9 ~ v3.3 (2026-08-05) - 정밀 정렬 규칙 & 폼 미니멀화
- **다중 정렬 우선순위 엔진**:
  - 1순위: 자주 구매한 순 (`useCount` 내림차순)
  - 2순위: 가나다순 (`localeCompare` 한국어 사전순)
- **입력 폼 미니멀 레이아웃**:
  - 상단 라벨 문구(`(선택)`, `(필수)`) 완전히 제거.
- **품목 초기화 안심 비활성화**:
  - 저장 품목의 무분별 삭제를 방지하기 위해 `품목 초기화` 버튼 및 기능 화면에서 비활성화.

### 💡 v2.1 ~ v2.8 (2026-08-05) - 상단 전광판 UI 혁신 & 스마트 추천 연동
- **전광판 스마트 추천 버튼 배치**:
  - 상단 전광판 좌측 하단에 `💡 스마트 추천` 버튼 탑재.
  - 클릭 시 즉시 추천 카드가 펼쳐지며 부드러운 스크롤 조작 연동.
- **예산 설정 팝업 모달 UI**:
  - 좌측 상단 `⚙️ 예산 설정` 버튼 및 인라인 `설정 완료` 버튼 모달 재설계.
- **전광판 현재 총합 1줄 정렬**:
  - `현재 총합 0원` 수평 Flex 1줄 정렬.

### 📝 v1.5 ~ v2.0 (2026-08-04) - 마크다운 동기화 & 모바일 파일 다운로드 대응
- **`품목_Emart.md` & `품목_코스트코.md` 마크다운 연동**:
  - 쉼표 구분자(`,`) 파싱 규칙 구축 및 `/api/items` 동기화 API 작성.
- **Vercel / Mobile Read-Only 파일시스템 대응**:
  - 서버 파일 쓰기 불가능한 환경에서 브라우저 Blob `.md` 즉시 다운로드 엔진 자동 전환.
- **터치 칩 롱 클릭 삭제**:
  - 550ms 롱 누름 시 확인 팝업 후 해당 품목 개별 삭제 기능 탑재.

### 🏁 v1.0 (2026-08-04) - 최초 마트 장보기 계산기 구축
- 이마트/코스트코 탭 전환, 예산 잔액/초과 전광판 실시간 계산, 장바구니 수량 조절 및 단위 변환(10=1만, 4.8=4.8천) 구축.
