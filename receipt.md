# 영수증 원문 구매내역 변환 프롬프트

아래 프롬프트는 코스트코/이마트 영수증처럼 품목명, 상품코드, 수량, 단가, 소계, 쿠폰 할인 행이 붙어서 입력된 원문을 Mart app 2의 구매내역 DB 저장용 구조로 변환할 때 사용한다.

## 사용 목적

- 영수증 OCR 또는 복사 원문을 구매내역 저장 데이터로 변환한다.
- 변환 결과는 `purchase_records.items_json`에 들어갈 `items` 배열과 `totalAmount` 계산에 맞춘다.
- `POST /api/save-record` 또는 `db.savePurchaseRecord(martName, timeStr, totalAmount, items)`에 바로 넘길 수 있는 형태로 만든다.
- 변환된 JSON은 `영수증_YYYY-MM-DD.json` 파일명으로 저장한다.
- 변환 결과에는 반드시 `martName`을 넣는다. 허용 값은 `코스트코`, `이마트`다.
- 구매일시는 반드시 포함한다. 영수증 원문이나 사용자 요청에 구매일시가 없으면 변환을 진행하지 말고 먼저 입력을 요청한다.
- 품명 중복 정리는 `품명_중복정리_이력.md`의 마트별 이력을 참고한다.

## 변환 원칙

1. 마트명은 필수다. 사용자가 지정한 값을 우선하고, 영수증 원문이나 요청에서 확인되지 않으면 `코스트코` 또는 `이마트` 중 어느 마트인지 먼저 물어본다.
2. 구매일시는 필수다. 사용자가 지정한 값을 우선하고, 영수증 원문이나 요청에 구매일시가 없으면 반드시 사용자에게 날짜/시간을 물어본 뒤 변환한다.
3. 상품 행은 `품목명 / 수량 / 단가 / 소계`로 분리한다.
4. 상품코드로 보이는 5-6자리 숫자는 저장 품목명에서 제거한다.
5. 품목명 안의 불필요한 전각 공백은 일반 공백으로 정규화한다.
6. 쿠폰/할인 행은 별도 음수 품목으로 저장하지 않는다. `*** CPN`, `IRC`, `- 2,000`처럼 표시된 할인은 대상 품목의 금액에서 차감해 최종 단가 또는 최종 소계로 반영한다.
7. 할인 적용 후 수량이 1이면 `price`는 할인 차감 후 최종 단가로 둔다.
8. 할인 적용 후 수량이 2 이상이면 총 할인 반영 금액을 수량으로 나눠 단가를 계산한다. 나누어 떨어지지 않으면 원문 소계를 우선하고 `warnings`에 기록한다.
9. `T`, `CPN`, `IRC`, `***`, 괄호성 쿠폰 표시, 상품코드, 특수기호, 품목명 안의 큰따옴표와 쉼표는 품목명에서 제거한다.
10. 같은 품목이 여러 줄 나오고 같은 단가 또는 같은 최종 단가가 확인되면 수량을 합산한다. 확실하지 않으면 원문 줄 단위로 유지하고 `warnings`에 적는다.
11. 단가와 소계가 맞지 않으면 할인 차감 후 최종 금액을 우선하고, `warnings`에 불일치를 기록한다.
12. 모든 금액은 쉼표 없는 숫자로 출력한다.
13. JSON 문법상 필요한 큰따옴표와 쉼표는 유지하되, 품목명 값 안에는 불필요한 큰따옴표와 쉼표를 넣지 않는다.
14. `martName`이 `코스트코`이면 코스트코 품명 중복 이력만 적용하고, `이마트`이면 이마트 품명 중복 이력만 적용한다.
15. 띄어쓰기, 대소문자, 하이픈 종류, 용량 표기 위치만 다른 품목은 같은 상품으로 보고 대표 품명으로 표준화한다. 예: `쇠고기육포280G` -> `쇠고기육포 280G`
16. DB 저장 전 같은 품목인지 비교할 때는 공백, 괄호, `_`, `-`, `–`, `—`를 제거한 비교 키를 함께 사용한다.

## DB 저장 안전 규칙

아래 규칙을 모두 만족해야 영수증 JSON을 업로드한다.

1. `martName`은 반드시 `코스트코` 또는 `이마트` 중 하나다. DB 저장 시 서버가 각각 `Costco`, `Emart`로 변환한다.
2. `timeStr`는 반드시 `YYYY-MM-DD HH:mm:ss` 형식이다. 예: `2026-06-05 21:57:00`
3. `items`는 1개 이상이어야 하며, 각 항목은 `name`, `price`, `quantity`만 가진다.
4. `name`은 빈 문자열이면 안 된다.
5. `name`에는 상품코드, 쿠폰 코드, `CPN`, `IRC`, 과세 표시 `T`, `***`, 큰따옴표, 쉼표, 줄바꿈을 넣지 않는다.
6. `name` 앞뒤 공백은 제거하고, 중간의 연속 공백은 한 칸으로 줄인다.
7. 용량/규격은 DB 중복 방지를 위해 가능한 한 기존 품목명과 같은 형식으로 쓴다. 예: `500ML X 8`, `2KG`, `1.3KG`, `200GX3`
8. `price`는 할인 차감 후 최종 단가이며 숫자만 사용한다. 예: `10890`
9. `quantity`는 양의 정수만 사용한다.
10. `totalAmount`는 모든 `price * quantity`의 합과 반드시 같아야 한다.
11. 쿠폰/할인은 `items`에 별도 품목으로 넣지 않는다. 대상 품목의 `price`에 차감 반영한다.
12. 동일 품목을 합산한 경우에도 `price`는 최종 단가, `quantity`는 합산 수량으로 저장한다.
13. 품목별 할인으로 최종 단가가 정수로 나누어 떨어지지 않으면 합산하지 말고 원문 줄 단위로 나누어 저장하고 `warnings`에 이유를 적는다.
14. DB에 저장하기 전 `items.reduce((sum, item) => sum + item.price * item.quantity, 0) === totalAmount`를 반드시 확인한다.
15. `warnings`는 저장에는 영향이 없지만, 병합/할인/인식이 애매한 판단 근거를 남긴다.

## 품목명 정제 순서

1. 전각 공백, 탭, 줄바꿈을 일반 공백으로 바꾼다.
2. 상품코드로 보이는 5-6자리 숫자를 제거한다.
3. `*** CPN`, `CPN`, `IRC`, 쿠폰번호, 과세 표시 `T`를 제거한다.
4. 품목명 안의 큰따옴표, 쉼표, 불필요한 별표와 특수기호를 제거한다.
5. 붙어 있는 용량은 읽기 쉬운 대표 품명 형식으로 정리한다. 예: `토마토4KG` -> `토마토 4KG`
6. `품명_중복정리_이력.md`에 같은 품목 후보가 있으면 마트별 대표 품명을 적용한다.
7. 최종 품명이 기존 품목표의 대표 품명과 다르면 `warnings`에 후보명을 적는다.
8. 대표 품명 적용 전후를 비교해, 공백이나 용량 표기 차이만 있으면 반드시 대표 품명으로 바꾼다.

## 대표 품명 정리 규칙

상세 이력은 `품명_중복정리_이력.md`를 기준으로 한다.

### 코스트코

- `KS SONOMA CHARDONN`, `와인 소노마 샤르도네`, `소노마 샤도네이`는 `소노마 샤르도네`로 정규화한다.
- `기린캔8개`, `기린캔500ML X 8`, `기린캔 500ML X 8`은 `기린캔 500ML X 8`로 정규화한다.
- `宮쇠고기육포280G`, `쇠고기육포280G`, `쇠고기육포 280G`는 `쇠고기육포 280G`로 정규화한다.
- `KS새우11-15 680G`, `새우11-15 680G`, `새우 11-15 680G`는 `새우 11-15 680G`로 정규화한다.
- `KS새우31-40 908G`, `새우31-40 908G`, `새우 31-40 908G`는 `새우 31-40 908G`로 정규화한다.
- `KS새우50-70 908G`, `새우50-70 908G`, `새우 50-70 908G`는 `새우 50-70 908G`로 정규화한다.
- `KS M.쇼비뇽블랑`은 `와인 소비뇽블랑`으로 정규화한다.
- `KAKUBIN WHISKY`, `산토리가쿠빈`, `산토리 카쿠빈`은 `산토리 카쿠빈`으로 정규화한다.
- 기존 등록 품목과 유사하면 새 품명을 만들지 말고 대표 품명을 사용한다.

### 이마트

- `더클래스 우유`, `더 클래스 우유`, `더클래식 우유`는 같은 2개 묶음 상품으로 확인되면 `더 클래스 우유(2개묶음)`으로 정규화한다.
- `서울우유2입`, `서울 우유 2입`은 `서울 우유 2입`으로 정규화한다.
- `청양고추`, `청양 고추`, `밀양 청양고추`는 모두 `청양고추`로 통일한다.
- `콩국물`, `국산 콩물`, `국산콩물`은 같은 상품으로 확인되면 `국산콩물`로 정규화한다.
- `파`, `대파`, `깐대파`는 손질/중량이 다르면 분리한다. 일반 대파이면 `대파`로 정규화한다.

## 출력 형식

반드시 아래 JSON 형식으로 변환하고, 결과를 `영수증_YYYY-MM-DD.json` 파일로 저장한다. `martName`과 `timeStr`는 필수이며, 마트명이 없으면 `코스트코` 또는 `이마트` 중 어느 마트인지 먼저 물어보고, 구매일시가 없으면 JSON을 만들지 말고 사용자에게 `YYYY-MM-DD HH:mm:ss` 형식으로 구매일시를 입력하라고 요청한다.

## Codex 처리 규칙

사용자가 `영수증 json으로 변환해줘`, `영수증을 json으로 전환해줘`처럼 영수증 JSON 변환을 요청하면 다음 순서로 처리한다.

1. 영수증 원문을 `martName`, `timeStr`, `items`, `totalAmount`, `warnings` 구조로 변환한다.
2. JSON 문법을 검증한다.
3. `items.reduce((sum, item) => sum + item.price * item.quantity, 0) === totalAmount` 합계를 검증한다.
4. 품목명에 상품코드, 쿠폰 코드, `CPN`, `IRC`, 큰따옴표, 쉼표, 줄바꿈이 남아 있지 않은지 확인한다.
5. 검증을 통과하면 구매일자의 날짜를 사용해 `영수증_YYYY-MM-DD.json` 파일로 저장한다.
6. 검증에 실패하면 파일로 저장하지 않고 불일치 사유와 수정이 필요한 항목을 먼저 보고한다.

```json
{
  "fileName": "영수증_YYYY-MM-DD.json",
  "martName": "코스트코",
  "timeStr": "YYYY-MM-DD HH:mm:ss",
  "items": [
    {
      "name": "품목명",
      "price": 0,
      "quantity": 1
    }
  ],
  "totalAmount": 0,
  "warnings": []
}
```

## 변환 프롬프트

```text
너는 Mart app 2의 구매내역 DB 저장용 데이터 변환기다.

아래 영수증 원문을 분석해서 `db.savePurchaseRecord(martName, timeStr, totalAmount, items)`에 넣을 수 있는 JSON으로 변환해라.

요구사항:
- 마트명은 필수이며 변환 결과 JSON의 `martName`에 반드시 넣는다.
- 마트명은 사용자 요청이나 영수증 원문에서 확인한 `코스트코` 또는 `이마트`를 사용한다.
- 마트명이 없으면 변환하지 말고, 먼저 `마트명을 코스트코 또는 이마트 중 하나로 입력해 주세요.`라고 요청한다.
- 구매일시는 필수다. 사용자가 지정한 값을 사용한다.
- 영수증 원문과 사용자 요청에 구매일시가 없으면 변환하지 말고, 먼저 `구매일시를 YYYY-MM-DD HH:mm:ss 형식으로 입력해 주세요.`라고 요청한다.
- 상품코드, 과세 표시 `T`, 불필요한 공백은 제거한다.
- 상품 행은 품목명, 수량, 단가, 소계로 해석한다.
- `*** CPN`, `IRC`, `- 금액`으로 표시된 쿠폰 할인은 별도 음수 품목으로 남기지 말고 대상 품목 금액에서 차감한다.
- `CPN`, `IRC`, `***`, 과세 표시 `T`, 상품코드, 특수기호, 품목명 안의 큰따옴표와 쉼표는 제거한다.
- 같은 품목명이 여러 번 나오면 원문상 같은 상품인지 판단하고, 확실하면 수량과 최종 금액을 합산한다. 확실하지 않으면 줄을 유지하고 `warnings`에 적는다.
- 각 품목은 `name`, `price`, `quantity`만 포함한다.
- `name`은 DB 저장 품목명이므로 빈 값, 상품코드, 쿠폰 코드, 큰따옴표, 쉼표, 줄바꿈을 포함하지 않는다.
- `name`은 `품명_중복정리_이력.md`의 대표 품명으로 표준화한다. 공백만 다른 `쇠고기육포280G`와 `쇠고기육포 280G` 같은 항목은 반드시 하나로 맞춘다.
- `price`는 할인 차감 후 최종 단가이고, `quantity`는 양의 정수다.
- 대표 품명 정리 규칙을 적용한다.
- 마트별 품명 중복 이력은 `품명_중복정리_이력.md`를 기준으로 적용한다. 다른 마트의 이력은 적용하지 않는다.
- 총액은 할인 차감 후 모든 `price * quantity`의 합으로 계산한다.
- JSON 출력 전 `items`의 합계가 `totalAmount`와 같은지 검산한다.
- 금액은 쉼표 없는 숫자로 출력한다.
- JSON 문법상 필요한 큰따옴표와 쉼표는 유지하되, 품목명 값 안의 불필요한 큰따옴표와 쉼표는 제거한다.
- 설명 문장 없이 JSON만 출력한다.
- 변환 결과는 구매일자의 날짜 부분을 사용해 `영수증_YYYY-MM-DD.json`으로 저장한다.

대표 품명 정리:
공통:
- `martName`이 `코스트코`이면 코스트코 규칙만 적용한다.
- `martName`이 `이마트`이면 이마트 규칙만 적용한다.

코스트코:
- `KS SONOMA CHARDONN`, `와인 소노마 샤르도네`, `소노마 샤도네이` -> `소노마 샤르도네`
- `기린캔8개`, `기린캔500ML X 8`, `기린캔 500ML X 8` -> `기린캔 500ML X 8`
- `宮쇠고기육포280G`, `쇠고기육포280G`, `쇠고기육포 280G` -> `쇠고기육포 280G`
- `KS새우11-15 680G`, `새우11-15 680G`, `새우 11-15 680G` -> `새우 11-15 680G`
- `KS새우31-40 908G`, `새우31-40 908G`, `새우 31-40 908G` -> `새우 31-40 908G`
- `KS새우50-70 908G`, `새우50-70 908G`, `새우 50-70 908G` -> `새우 50-70 908G`
- `KS M.쇼비뇽블랑` -> `와인 소비뇽블랑`
- `KAKUBIN WHISKY`, `산토리가쿠빈`, `산토리 카쿠빈` -> `산토리 카쿠빈`

이마트:
- `더클래스 우유`, `더 클래스 우유`, `더클래식 우유` -> `더 클래스 우유(2개묶음)` 단, 같은 2개 묶음 상품일 때만 적용
- `서울우유2입`, `서울 우유 2입` -> `서울 우유 2입`
- `콩국물`, `국산 콩물`, `국산콩물` -> `국산콩물` 단, 같은 상품일 때만 적용
- `파`, `대파`, `깐대파`는 손질/중량이 다르면 분리

마트명:
{마트명 - 코스트코 또는 이마트, 없으면 사용자에게 먼저 입력 요청}

구매일시:
{구매일시 - 없으면 사용자에게 먼저 입력 요청}

영수증 원문:
{영수증 원문}
```

## 예시 입력

```text
구매일시: 2026-08-05 21:00:00
영수증 원문:
토마토4KG512905111,89011,890미니 대추토마토685392112,89012,890*** CPN대추토마토IRC2341512,000- 2,000
```

## 예시 출력

```json
{
  "fileName": "영수증_2026-08-05.json",
  "martName": "코스트코",
  "timeStr": "2026-08-05 21:00:00",
  "items": [
    {
      "name": "토마토 4KG",
      "price": 11890,
      "quantity": 1
    },
    {
      "name": "미니 대추토마토",
      "price": 10890,
      "quantity": 1
    }
  ],
  "totalAmount": 22780,
  "warnings": []
}
```
