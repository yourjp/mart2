\# 프로젝트 명: Node.js 기반 마트 장보기 예산 계산기 웹 앱 2





\## Agent 운영 계획





이 프로젝트는 총 3개의 agent로 나누어 개발하는 것을 권장한다.





\### Agent 1: 앱 구조 및 백엔드 담당





\- Node.js v18+ 기반 Express 프로젝트를 구성한다.





\- `package.json`, `server.js`, 정적 파일 제공 구조를 만든다.





\- `public/index.html`, `public/styles.css`, `public/app.js`, `README.md` 기본 구조를 준비한다.





\- `npm install` 후 `npm start`로 `http://localhost:3000`에서 앱이 실행되도록 한다.





\### Agent 2: 프론트엔드 UI 및 모바일 UX 담당





\- 모바일 우선 화면을 구현한다.





\- 상단 전광판 영역, 예산 입력, 품목 입력, 가격 입력, 추천 목록, 장바구니 리스트 UI를 만든다.





\- 한 손 조작에 적합한 큰 버튼, 큰 입력창, 고대비 색상, 큰 숫자 표시를 적용한다.





\- 화면이 좁아도 텍스트와 버튼이 겹치거나 넘치지 않도록 반응형 CSS를 작성한다.





\### Agent 3: 상태 관리, 계산 로직, 검증 담당





\- 예산, 장바구니, 저장 품목, 가격 이력을 localStorage에 저장한다.





\- 빠른 가격 입력 규칙, 가격 변화 판정, 품목 추천, 데이터 마이그레이션 로직을 구현한다.





\- 동일 품목 가격 갱신, 가격 이력 최근 20개 제한, 자동 품목명 처리 로직을 검증한다.





\- 완료 기준에 맞춰 수동 테스트 체크리스트를 수행하고 README를 보강한다.





\## 목표





마트에서 장을 볼 때 모바일 브라우저로 물건 가격을 빠르게 입력하고, 현재 총액과 예산 대비 남은 금액 또는 초과 금액을 즉시 확인할 수 있는 웹 앱을 만든다.





앱은 Node.js + Express 서버로 실행되며, 모바일 한 손 조작에 최적화된 큰 글씨, 큰 버튼, 고대비 UI를 제공해야 한다.





\## 기술 스택





\- Runtime: Node.js v18+





\- Backend: Express.js





\- Frontend: Vanilla HTML, CSS, JavaScript





\- Styling: 커스텀 CSS





\- Data Persistence: 브라우저 localStorage





\- Deployment: Vercel





\- 별도 데이터베이스는 사용하지 않는다.





\## Vercel 배포 및 데이터 저장 기준





이 앱은 Vercel에 배포할 수 있다.





데이터 저장은 서버 파일 시스템이나 데이터베이스가 아니라 브라우저 `localStorage`를 사용한다. 따라서 Vercel에 배포해도 예산, 장바구니, 저장 품목, 가격 이력은 정상 저장된다.





단, 저장 범위는 다음과 같다.





\- 같은 브라우저, 같은 기기에서 다시 접속하면 데이터가 유지된다.





\- 휴대폰 Chrome에서 입력한 데이터는 같은 휴대폰 Chrome에 유지된다.





\- PC 브라우저, 다른 휴대폰, 다른 브라우저와는 데이터가 자동 공유되지 않는다.





\- 브라우저 사이트 데이터 삭제, 시크릿 모드 종료, localStorage 삭제 시 저장 데이터가 사라질 수 있다.





\- Vercel의 서버리스 환경에서는 서버 측 JSON 파일 저장을 사용하지 않는다.





\- 여러 기기 간 동기화나 사용자 계정별 저장이 필요하면 향후 Supabase, Firebase, Vercel Postgres 같은 외부 데이터베이스를 추가한다.





이번 버전에서는 localStorage를 정식 저장 방식으로 사용하며, 품명과 가격 이력도 localStorage에 저장해야 한다.





\## 주요 화면 구성





\### 1. 상단 전광판 영역





모바일 화면 상단 약 30% 이상을 사용하여 다음 정보를 크게 표시한다.





\- 현재 총합





&#x20; - 예: `35,800원`





\- 예산 대비 상태





&#x20; - 예산 내일 때: `24,200원 남음`





&#x20; - 예산 초과일 때: `12,500원 초과`





\- 상태별 색상





&#x20; - 예산 내: 녹색 또는 파란색 계열





&#x20; - 예산 초과: 강한 빨간색 경고 테마





숫자는 반드시 천 단위 쉼표와 `원` 단위를 포함해 표시한다.





\## 2. 예산 설정





\- 기본 예산은 `60,000원`으로 설정한다.





\- 사용자는 예산을 수정할 수 있다.





\- 예산 수정 시 현재 총합과 남은 금액 또는 초과 금액이 즉시 다시 계산되어야 한다.





\- 입력값은 숫자만 허용한다.





\## 3. 품목 빠른 입력





사용자는 다음 값을 입력해 장바구니에 항목을 추가할 수 있다.





\- 품목명: 선택 입력





\- 가격: 필수 입력





동작 조건:





\- 품목명을 비우면 `품목 1`, `품목 2`, `품목 3` 형식으로 자동 생성한다.





\- 자동 생성된 품목에는 `isAutoName: true`를 둔다.





\- 자동 생성 품목은 가격 이력 저장 대상에서 제외한다.





\- 품목명을 입력한 경우 해당 품목명과 가격을 localStorage에 저장한다.





\- 저장된 품목명은 이후 장보기 계산에서 재사용할 수 있어야 한다.





\- 가격 입력 후 Enter 키를 누르면 즉시 항목을 추가한다.





\- 항목 추가 후 가격 입력창은 비워지고 다시 포커스된다.





\- 가격은 숫자만 입력 가능해야 한다.





\- 0원 이하 또는 빈 가격은 추가하지 않는다.





\## 4. 빠른 가격 입력 규칙





가격 입력은 마트에서 빠르게 입력할 수 있도록 축약 입력을 지원한다.





\### 입력 해석 규칙





\- 사용자가 `1`처럼 한 자리 숫자를 입력하면 `1,000원`으로 해석한다.





\- 사용자가 `10`처럼 두 자리 숫자를 입력하면 `10,000원`으로 해석한다.





\- 사용자가 `100` 이상을 입력하면 입력값 그대로 원 단위 금액으로 해석한다.





예시:





\- `1` -> `1,000원`





\- `2` -> `2,000원`





\- `10` -> `10,000원`





\- `35` -> `35,000원`





\- `990` -> `990원`





\- `1200` -> `1,200원`





\- `10000` -> `10,000원`





\### 소수점 입력 규칙





소수점(`.`)이 포함된 입력은 "천원" 단위 소수로 해석한다. 정수부는 천원 단위, 소수부는 천원 미만(백/십/일 단위) 금액을 나타낸다.





\- `4.8` -> `4,800원`





\- `.5` -> `500원`





\- `12.34` -> `12,340원`





\### 입력 보조 표시





\- 가격 입력창 근처에 현재 입력값이 실제 얼마로 해석되는지 즉시 보여준다.





\- 예: 사용자가 `10`을 입력하면 `10,000원으로 입력됩니다` 표시.





\- 빈 값이거나 유효하지 않은 값이면 보조 표시를 숨긴다.





\### 로직 주의사항





\- 가격 비교, 총합 계산, 가격 이력 저장에는 해석된 실제 금액을 사용한다.





\- 장바구니에는 원본 입력값이 아니라 해석된 금액을 저장한다.





\- `10,000`, `10000원`처럼 쉼표나 원 단위가 포함된 입력도 처리한다.





\- 숫자가 아닌 문자는 제거한 뒤 해석한다.





\- 0원 이하로 해석되는 값은 추가하지 않는다.





\## 5. 장바구니 리스트





추가된 항목을 목록으로 표시한다.





각 항목에는 다음 정보를 표시한다.





\- 품목명





\- 단가





\- 수량





\- 품목별 합계





\- 가격 변화 상태





\- 수량 증가 버튼 `+`





\- 수량 감소 버튼 `-`





\- 삭제 버튼





동작 조건:





\- `+` 버튼을 누르면 수량이 1 증가한다.





\- `-` 버튼을 누르면 수량이 1 감소한다.





\- 수량이 1인 상태에서 `-`를 누르면 항목을 삭제한다.





\- 삭제 버튼을 누르면 해당 항목을 삭제한다.





\- 전체 초기화 버튼을 제공한다.





\- 전체 초기화 전에는 `confirm()`으로 확인한다.





\## 6. 저장된 기본 품목





앱 최초 실행 시 localStorage에 저장된 품목 데이터가 없으면 다음 기본 품목을 미리 등록한다.





\- 맥주





\- 행사 우유





\- 소화 우유





\- 계란





\- 돼지고기





\- 파





\- 상추





\- 깻잎





\- 콩국물





기본 품목 데이터 구조는 다음과 같이 둔다.





```js





{





&#x20; name: "맥주",





&#x20; lastPrice: null,





&#x20; useCount: 0,





&#x20; lastUsedAt: null,





&#x20; isDefault: true,





&#x20; priceHistory: \[]





}





```





동작 조건:





\- 기본 품목은 품목명 자동완성 또는 추천 목록에 표시한다.





\- 기본 품목을 선택하면 품목명만 자동 입력한다.





\- `lastPrice`가 있는 품목은 가격도 함께 자동 입력한다.





\- 기본 품목이라도 사용자가 가격을 입력해 추가하면 `lastPrice`, `useCount`, `lastUsedAt`, `priceHistory`를 갱신한다.





\- 장바구니 전체 초기화 시 기본 품목 목록은 삭제하지 않는다.





\- 저장된 품목 목록 초기화 시 기본 품목은 다시 복원한다.





\## 7. 품목명 추천 및 자동완성





품목명 입력창은 저장된 기본 품목과 사용자가 저장한 품목을 기반으로 자동완성 추천을 제공한다.





\### 추천 표시 조건





\- 사용자가 품목명 입력창에 한 글자 이상 입력하면 추천 목록을 표시한다.





\- 입력값과 유사한 저장 품목을 추천한다.





\- 한글자만 입력해도 유사한 품목이 있으면 추천해야 한다.





\- 입력값이 비어 있으면 기본 품목 또는 최근 사용 품목을 추천할 수 있다.





\### 추천 기준





추천은 다음 기준을 조합해 정렬한다.





1\. 입력값으로 시작하는 품목





2\. 입력값을 포함하는 품목





3\. 초성 또는 부분 유사성이 있는 품목





4\. 최근 사용한 품목





5\. 자주 사용한 품목





예:





\- 입력값 `우` -> `행사 우유`, `소화 우유`





\- 입력값 `소` -> `소화 우유`





\- 입력값 `상` -> `상추`





\- 입력값 `깻` -> `깻잎`





\- 입력값 `ㄱ` -> `계란`, `깻잎`, `콩국물` 등 초성 유사 품목





\### 선택 동작





\- 추천 품목을 선택하면 품목명 입력창에 해당 품목명이 입력된다.





\- 선택한 품목에 `lastPrice`가 있으면 가격 입력창에도 최신 가격을 자동 입력한다.





\- 선택한 품목에 `lastPrice`가 없으면 가격 입력창은 비워둔다.





\- 추천 품목 선택 후 가격 입력창으로 포커스를 이동한다.





\### UI 요구사항





\- 추천 목록은 모바일에서 누르기 쉽게 큰 터치 영역으로 표시한다.





\- 추천 품목에는 가능하면 최근 가격도 함께 표시한다.





&#x20; - 예: `맥주 · 최근 12,000원`





&#x20; - 가격이 없으면 품목명만 표시한다.





\- 추천 목록은 너무 길어지지 않도록 최대 5\~8개까지만 표시한다.





\## 8. 저장된 품목 관리 및 재사용





\- 사용자가 직접 입력한 품목명과 가격은 `savedItems` 형태로 localStorage에 저장한다.





\- 동일 품목명은 `trim()` 처리 후 완전 일치 기준으로 판단한다.





\- 동일한 품목명이 다시 입력되면 최근 입력 가격으로 갱신한다.





\- 저장된 품목명은 품목명 자동완성 또는 추천 목록에 표시한다.





\- 저장된 품목을 선택하면 품목명과 최근 가격이 자동 입력된다.





\- 저장된 품목 데이터는 장바구니 초기화와 별개로 유지한다.





\- 필요 시 저장된 품목 목록을 초기화할 수 있는 기능을 제공한다.





\## 9. 동일 품목 가격 갱신 규칙





\- 동일 품목명은 `trim()` 처리 후 완전 일치 기준으로 판단한다.





\- 동일 품목명이 이미 저장되어 있고 새 가격이 입력되면 기존 `lastPrice`를 새 가격으로 덮어쓴다.





\- 기존 가격은 삭제하지 않고 `priceHistory`에 이력으로 유지한다.





\- 가격 변화 판정은 `lastPrice`를 덮어쓰기 전에 수행한다.





\- 장바구니 자동완성에서 해당 품목을 다시 선택하면 갱신된 최신 가격이 입력된다.





예:





\- 기존 저장 품목: `맥주`, `lastPrice: 12,000`





\- 새 입력: `맥주`, `price: 11,000`





\- 결과:





&#x20; - `lastPrice`는 `11,000`으로 갱신





&#x20; - `priceHistory`에는 `12,000`, `11,000` 기록 유지





&#x20; - 장바구니에는 `할인 1,000원`으로 표시





\## 10. 가격 이력 및 가격 변화 표시





앱은 품목별 가격 이력을 localStorage에 저장해야 한다.





\### 저장 데이터 구조





저장된 품목은 가격 이력을 포함할 수 있도록 다음 구조로 관리한다.





```js





{





&#x20; name: "맥주",





&#x20; lastPrice: 12000,





&#x20; useCount: 3,





&#x20; lastUsedAt: "2026-08-04T00:00:00.000Z",





&#x20; isDefault: true,





&#x20; priceHistory: \[





&#x20;   {





&#x20;     price: 12000,





&#x20;     usedAt: "2026-08-04T00:00:00.000Z"





&#x20;   }





&#x20; ]





}





```





\### 가격 이력 저장 조건





\- 사용자가 품목명과 가격을 입력해 장바구니에 추가하면 해당 품목의 가격 이력을 저장한다.





\- 동일 품목이 이미 저장되어 있으면 `priceHistory`에 새 가격 기록을 추가한다.





\- `lastPrice`는 가장 최근 입력 가격으로 갱신한다.





\- `useCount`는 품목 추가 시 1 증가한다.





\- `lastUsedAt`은 가장 최근 추가 시각으로 갱신한다.





\- 품목명 없이 자동 생성된 `품목 1`, `품목 2` 등은 가격 이력 저장 대상에서 제외한다.





\- 가격 이력은 새 품목을 입력해 장바구니에 추가할 때만 저장하며, 수량 변경 시에는 저장하지 않는다.





\- 가격 이력은 품목별 최근 20개까지만 유지한다.





\### 가격 변화 판정





품목을 추가할 때 이전 가격과 현재 입력 가격을 비교해 가격 변화를 표시한다.





중요:





\- 가격 변화는 저장 품목의 `lastPrice`를 갱신하기 전에 계산한다.





\- 계산된 가격 변화 정보는 장바구니 항목에 함께 저장하여 렌더링 시 결과가 바뀌지 않도록 한다.





판정 기준:





\- 이전 가격보다 현재 가격이 낮으면: `할인`





\- 이전 가격보다 현재 가격이 높으면: `인상`





\- 이전 가격과 현재 가격이 같으면: `동일`





\- 이전 가격이 없으면: `첫 기록`





장바구니 항목 데이터 예시:





```js





{





&#x20; id: "item-001",





&#x20; name: "맥주",





&#x20; price: 11000,





&#x20; quantity: 1,





&#x20; isAutoName: false,





&#x20; priceChange: {





&#x20;   type: "discount",





&#x20;   amount: 1000





&#x20; }





}





```





\### 가격 변화 표시 방식





장바구니 리스트의 각 품목 옆에 가격 변화 상태를 표시한다.





예시:





\- `맥주 12,000원 · 할인 1,000원`





\- `계란 7,500원 · 인상 500원`





\- `파 2,000원 · 동일`





\- `콩국물 3,500원 · 첫 기록`





상태별 시각적 표현:





\- 할인: 파란색 또는 녹색 배지





\- 인상: 빨간색 배지





\- 동일: 회색 배지





\- 첫 기록: 중립 색상 배지





\## 11. 향후 목표가격 미달 제안 기능을 고려한 데이터 구조





향후 예산보다 총합이 낮을 때 추가 구매 후보를 제안할 수 있도록 저장 품목 데이터를 설계한다.





향후 제안 기능의 기본 아이디어:





\- 현재 총합이 예산보다 낮을 때 남은 금액 이하의 저장된 품목을 추천할 수 있다.





\- 자주 사용한 품목(`useCount`)을 우선 추천할 수 있다.





\- 최근 사용한 품목(`lastUsedAt`)을 우선 추천할 수 있다.





\- 최근 할인된 품목을 우선 추천할 수 있다.





\- 평균 가격 대비 현재 가격이 낮은 품목을 우선 추천할 수 있다.





이번 버전에서는 자동 제안 기능을 실제 구현하지 않아도 되며, 저장 데이터 구조와 UI 확장 가능성만 고려한다.





\## 12. 저장 동작





\- 예산, 장바구니 항목, 저장된 품목 목록은 localStorage에 저장한다.





\- 브라우저를 새로고침해도 기존 예산, 장바구니 상태, 저장 품목, 가격 이력이 유지되어야 한다.





\- Vercel 배포 환경에서도 저장 방식은 동일하게 브라우저 localStorage를 사용한다.





\- 사용자가 입력한 품명은 저장 품목 목록에 유지한다.





\- 사용자가 입력한 품명의 가격은 `lastPrice`로 저장한다.





\- 동일 품명의 이전 가격들은 `priceHistory`에 최근 20개까지 저장한다.





\- 서버 측 JSON 파일 저장이나 Vercel 함수 내부 파일 저장은 사용하지 않는다.





\- 장바구니 비우기 시 장바구니 데이터만 초기화한다.





\- 저장 품목 초기화 시 저장 품목과 가격 이력을 초기화하고 기본 품목은 다시 복원한다.





\## 13. localStorage 데이터 검증 및 마이그레이션





앱 시작 시 localStorage 데이터를 검증하고 누락 필드는 기본값으로 보정한다.





예:





```js





{





&#x20; name,





&#x20; lastPrice: item.lastPrice ?? null,





&#x20; useCount: item.useCount ?? 0,





&#x20; lastUsedAt: item.lastUsedAt ?? null,





&#x20; isDefault: item.isDefault ?? false,





&#x20; priceHistory: Array.isArray(item.priceHistory) ? item.priceHistory : \[]





}





```





주의사항:





\- 예전 버전 데이터에 `priceHistory`, `isDefault`, `useCount` 등이 없어도 앱이 오류 없이 실행되어야 한다.





\- 잘못된 JSON 또는 손상된 localStorage 데이터가 있으면 안전하게 기본값으로 복구한다.





\## 14. 모바일 UX 요구사항





\- 모바일 우선 반응형 디자인으로 작성한다.





\- 한 손으로 누르기 쉽도록 버튼과 입력창은 충분히 크게 만든다.





\- 가격 입력이 가장 빠른 작업이므로 가격 입력창 접근성을 가장 우선한다.





\- 실외 마트 환경에서도 읽기 쉽도록 고대비 색상을 사용한다.





\- 화면이 좁아도 텍스트가 버튼이나 카드 밖으로 넘치지 않아야 한다.





\## 15. 접근성 및 사용성





\- 주요 버튼에는 명확한 텍스트 또는 이해하기 쉬운 아이콘을 사용한다.





\- 입력 필드에는 label 또는 aria-label을 제공한다.





\- 키보드 Enter 입력으로 빠른 추가가 가능해야 한다.





\- 숫자 입력 필드는 모바일에서 숫자 키패드가 뜨도록 설정한다.





\## 16. 프로젝트 구조





다음 구조로 작성한다.





```text





shopping-budget-app/





|- package.json





|- server.js





|- vercel.json





|- .gitignore





|- public/





|  |- index.html





|  |- styles.css





|  `- app.js





|- 구매내역.md          (계산결과 저장 기능이 로컬 실행 시 생성/누적하는 런타임 파일)





`- README.md





```





\## 17. 서버 요구사항





\- Express 서버를 사용한다.





\- `public` 폴더를 정적 파일로 제공한다.





\- 기본 포트는 `3000`으로 한다.





\- `POST /api/save-record` 엔드포인트를 제공하여 계산결과를 `구매내역.md`에 이어쓰기(append)한다. (22. 계산결과 저장 기능 참고)





\- 서버 실행 명령어:





&#x20; - `npm install`





&#x20; - `npm start`





\## 18. Vercel 배포 요구사항





\- Vercel에서 배포 가능하도록 프로젝트를 구성한다.





\- Express 앱은 Vercel 서버리스 함수 또는 Vercel이 지원하는 Node.js 실행 방식에 맞게 동작해야 한다.





\- 정적 파일은 `public` 폴더에서 제공한다.





\- 배포 후 앱 URL에 접속하면 동일한 기능을 사용할 수 있어야 한다.





\- 데이터 저장은 Vercel 서버가 아니라 브라우저 localStorage에 저장한다.





\- Vercel 배포 후에도 같은 브라우저에서 새로고침하거나 다시 접속했을 때 예산, 장바구니, 저장 품목, 가격 이력이 유지되어야 한다.





\## 19. README 요구사항





README에는 다음 내용을 포함한다.





\- 프로젝트 설명





\- 설치 방법





\- 실행 방법





\- Vercel 배포 방법





\- localStorage 저장 방식 설명





\- 품명 및 가격 이력이 같은 브라우저에 저장된다는 설명





\- 사용 방법





\- 주요 기능 목록





\## 20. 완료 기준





다음 조건을 모두 만족해야 한다.





\- `npm install` 후 `npm start`로 서버가 실행된다.





\- 브라우저에서 `http://localhost:3000` 접속 시 앱이 보인다.





\- Vercel 배포 후 배포 URL에서도 앱이 정상 동작한다.





\- 기본 예산이 `60,000원`으로 표시된다.





\- 예산 수정, 품목 추가, 수량 변경, 삭제, 장바구니 비우기가 정상 동작한다.





\- 총합과 예산 대비 금액이 모든 조작마다 즉시 갱신된다.





\- 품목명 없이 가격만 입력하면 자동 품목명이 생성된다.





\- 기본 품목 목록이 자동완성 또는 추천 목록에 표시된다.





\- 품목명 입력창에 한 글자만 입력해도 유사한 저장 품목이 추천된다.





\- 추천 품목을 선택하면 품목명과 최신 가격이 자동 입력된다.





\- 동일 품목의 새 가격 입력 시 `lastPrice`가 새 가격으로 갱신된다.





\- 동일 품목의 기존 가격은 `priceHistory`에 유지된다.





\- 가격 변화가 `할인`, `인상`, `동일`, `첫 기록`으로 표시된다.





\- 가격 입력 `1`은 `1,000원`, `10`은 `10,000원`으로 해석된다.





\- 로컬 실행과 Vercel 배포 환경 모두에서 새로고침 후 localStorage를 통해 예산, 장바구니, 저장 품목, 가격 이력이 유지된다.





\- 품명과 가격 이력은 서버 파일이 아니라 브라우저 localStorage에 저장된다.





\- 저장 품목 초기화 시 기본 품목이 다시 복원된다.





\- 모바일 화면에서 큰 글씨와 큰 버튼으로 사용 가능하다.





\- 이마트/코스트코 마트 탭을 전환하면 예산, 장바구니, 저장 품목, 가격 이력이 마트별로 독립적으로 유지된다.





\- 마트 탭 전환 시 전광판과 탭 색상이 마트별로 즉시 달라진다.





\- 예산 초과 경고 색상은 마트 테마와 무관하게 항상 동일한 빨간색으로 표시된다.





\- 장바구니에 품목이 있는 상태에서 "계산결과 저장" 버튼을 누르면 로컬 서버 환경에서 `구매내역.md`에 날짜, 마트명, 품목, 단가, 수량, 소계, 총합계가 누적 기록된다.





\- 장바구니가 비어 있을 때 "계산결과 저장"을 누르면 저장하지 않고 안내 문구를 표시한다.





\## 21. 마트별(이마트/코스트코) 관리





마트에 따라 물가와 예산 계획이 달라질 수 있으므로, 두 개의 마트를 독립적으로 관리할 수 있어야 한다.





\### 마트 탭





\- 화면 상단에 `이마트`, `코스트코` 두 개의 탭을 표시한다.





\- 탭을 눌러 현재 관리 중인 마트를 전환할 수 있다.





\- 마지막으로 선택한 마트는 localStorage에 저장되어 새로고침 후에도 유지된다.





\### 데이터 분리





다음 데이터는 마트별로 완전히 분리하여 localStorage에 저장한다.





\- 예산 (`martApp\_budget\_이마트`, `martApp\_budget\_코스트코`)





\- 장바구니 (`martApp\_cart\_이마트`, `martApp\_cart\_코스트코`)





\- 저장 품목 및 가격 이력 (`martApp\_savedItems\_이마트`, `martApp\_savedItems\_코스트코`)





동작 조건:





\- 한쪽 마트에서 품목을 추가/삭제/수량 변경해도 다른 마트의 데이터에는 영향을 주지 않는다.





\- 각 마트는 독립적으로 기본 품목(맥주, 행사 우유 등)을 최초 실행 시 시딩한다.





\- 장바구니 비우기, 저장 품목 초기화 등 버튼은 현재 선택된 마트에만 적용된다.





\- 마트 전환 이전 버전(단일 마트) 사용자의 기존 localStorage 데이터는 `이마트` 데이터로 자동 이전(마이그레이션)한다.





\### 마트별 색상 테마





\- 마트를 전환하면 전광판 배경색과 탭 강조색이 마트별로 다르게 표시되어 한눈에 구분할 수 있어야 한다.





&#x20; - 이마트: 파란색 계열





&#x20; - 코스트코: 주황/브라운 계열





\- 예산 초과 경고(빨간색)는 마트 테마 색상보다 우선하여 항상 동일하게 표시한다.





\- 페이지 로드 시 색상이 잠깐 바뀌는 현상(FOUC)을 방지하기 위해 저장된 마트 값을 초기 렌더링 전에 적용한다.





\## 22. 계산결과 저장 기능 (구매내역.md 누적 기록)





장을 본 결과를 텍스트 기록으로 남기고 싶을 때를 위해, 서버의 로컬 파일에 계산 결과를 누적 저장하는 기능을 제공한다.





\### UI





\- 장바구니 영역 하단에 "계산결과 저장" 버튼을 둔다.





\- 버튼 아래에 이 기능이 로컬 서버 실행 환경에서만 동작한다는 안내 문구를 표시한다.





\### 동작 조건





\- 버튼을 누르면 현재 마트명, 장바구니 품목 목록(품목명, 단가, 수량), 총합계를 서버로 전송한다.





\- 장바구니가 비어 있으면 서버로 전송하지 않고 안내 메시지만 표시한다.





\- 서버는 요청을 받아 날짜/시간, 마트명, 품목별 표, 총합계를 마크다운 형식으로 만들어 프로젝트 루트의 `구매내역.md` 파일에 이어쓰기(append)한다. 기존 기록은 삭제하지 않고 계속 누적한다.





\- 저장에 성공하면 성공 안내를, 실패하면(예: Vercel 서버리스 환경처럼 파일 시스템 쓰기가 불가능한 경우) 실패 안내를 표시한다.





\### 저장 형식 예시





```md





\## 2026-08-04 10:12:29 (이마트)





| 품목 | 단가 | 수량 | 소계 |





|---|---|---|---|





| 맥주 | 12,000원 | 1 | 12,000원 |





| 계란 | 7,500원 | 2 | 15,000원 |





\*\*총합계: 27,000원\*\*





\---





```





\### 제약 사항





\- 이 기능은 서버 파일 시스템에 직접 쓰기 때문에, Vercel과 같은 서버리스 배포 환경에서는 파일이 영구적으로 저장되지 않을 수 있다. 이 기능은 로컬(`npm start`) 실행 환경 전용 보조 기능으로 취급하며, 예산/장바구니/저장 품목 등 앱의 핵심 데이터 저장 방식(localStorage)에는 영향을 주지 않는다.





\- `구매내역.md`는 저장소에 커밋하지 않는 런타임 생성 파일로 `.gitignore`에 포함한다.





\## 23. 예산 초과 시 품목 추천





예산을 초과했을 때 어떤 품목을 빼면 예산에 가장 가깝게 맞출 수 있는지 안내한다.





\### 동작 조건





\- 총합이 예산을 초과하면(전광판이 `초과` 상태) 전광판 바로 아래에 추천 배너를 표시한다.





\- 초과 금액(총합 - 예산)과 가장 가까운 소계(단가 x 수량)를 가진 장바구니 품목 하나를 찾아 추천한다.





\- 추천 문구에는 품목명, 소계, 초과 금액과의 차이를 함께 표시한다.





&#x20; - 예: `'상추' 소계 3,000원을(를) 빼면 예산 초과액과 가장 가까워집니다 (차이 500원)`





\- 추천 배너에는 "이 품목 빼기" 버튼을 제공하여 한 번의 클릭으로 해당 품목을 장바구니에서 제거할 수 있다.





\- 품목 제거, 수량 변경 등으로 예산 이내로 돌아오면 추천 배너는 즉시 사라진다.





\- 장바구니가 비어 있으면 추천 배너를 표시하지 않는다.


## 24. GitHub 배포 정책 (Deployment Policy)

- 사용자가 **"배포"**라고 명령하거나 요청할 경우에만 **GitHub 원격 저장소(`https://github.com/yourjp/mart2.git`)의 `main` 브랜치로 최신 코드 스테이징, 커밋 및 푸시(`git push -u origin main`)를 실행하는 것**을 의미한다.
- 일반적인 수정이나 기능 추가 요청 시에는 로컬에서만 작업 및 검증을 완료하며, **사용자가 명시적으로 배포 요청을 한 경우에만** GitHub 푸시를 수행한다.
- 배포 전에는 최소 검증으로 `node --check public/app.js`와 `node --check server.js`를 실행한다.


## 25. 버전 및 수정 날짜 표시 규칙 (Version & Modified Date Policy)

- 앱 코드를 수정할 때마다 `version x.x (YY-MM-DD)` 형식(예: `v1.1 (26-08-04)`)으로 버전을 올리고 수정한 날짜를 함께 표기한다.
- 전광판 우측 상단(`#app-version-badge`), `public/app.js`(`APP_VERSION`), `package.json`, `package-lock.json`, `history.md`에 항상 동기화하여 시각적으로 보여준다.


## 26. 마크다운 파일 기반 품목 관리 기능 (Markdown Item Management)

- 마트별 등록 품목 및 가격 데이터는 `품목_Emart.md` 및 `품목_코스트코.md` 마크다운 파일 형태로 연동/업데이트가 가능하다.
- 표(Table) 형식을 지양하고 `,`(쉼표) 구분 형식(`품목명, 단가` 예: `맥주(5개묶음), 14,000원`)으로 작성하여 수정 시 웹 앱 로딩 시 최신 품목 및 가격 정보가 자동 연동된다.
- 단가에 천 단위 쉼표가 포함되므로 파일 파싱 시 줄의 첫 번째 쉼표를 품목명/단가 구분자로 사용하고, 그 뒤 전체를 단가 문자열로 읽는다.
- 품목을 신규 등록하거나 파일로 업로드하기 전에는 기존 등록 품목과 유사/중복 여부를 먼저 검토한다.
- 동일 품목으로 판단되면 새 품목명을 추가하지 않고 기존 대표 품명으로 단가만 갱신한다.
- 업로드 파일 안에서도 동일 품목의 접두어, 띄어쓰기, 용량 표기, 한글/외래어 표기 차이를 확인하고 하나의 대표 품명으로 정리한 뒤 업로드한다.
- 품목 파일 업로드 후 서버가 `.md` 파일을 다시 생성할 때도 대표 품명 규칙을 적용해 동일 품목이 여러 이름으로 저장되지 않도록 한다.


## 27. 전광판 레이아웃 및 스마트 추천 버튼 배치 (Dashboard Board Layout & Smart Recommendation)

- 전광판(`dashboard-board`) 영역배치:
  - **좌측 상단**: `⚙️ 예산 설정` 버튼 (`.btn-budget-setting`)
  - **우측 상단**: 버전 표시 배지 (`#app-version-badge`, 현재 `v1.3.82 (26-08-08)`)
  - **좌측 하단**: `💡 스마트 추천` 버튼 (`.btn-smart-recommendation`)
  - **우측 하단**: Neon DB 로고 배지 (`.db-badge.neon-postgres-badge`)
- `현재 총합 0원` 표시는 가로 한 줄(Single Line Flex)로 정렬된다.
- `💡 스마트 추천` 버튼과 예산 상태 표시 영역(`.status-display`) 간의 시각적 겹침을 방지하기 위해 예산 상태 영역은 우측 오프셋(36px) 정렬을 유지한다.
- `💡 스마트 추천` 버튼 클릭 시 예산 70% 임계값 조건과 무관하게 즉시 맞춤 품목 추천 카드가 활성화되어 표시된다.


## 28. 스마트 추천 및 등록 품목 정렬 정밀 규칙 (Item Sorting Rules)

- **스마트 품목 추천 카드 (`withinBudgetBanner`) 정렬 순서 (옵션 B)**:
  - **후보 조건**: 아직 장바구니에 담기지 않은 미담김 품목 중 **`단가 <= 남은 예산` (`설정 예산 - 현재 총액`) 조건**을 엄격히 충족하는 품목만 대상으로 추천한다.
  1. **1순위 (구매 빈도)**: 장바구니에 담은 구매 횟수가 가장 많은 품목 (`useCount` 내림차순)
  2. **2순위 (가나다순)**: 한국어 사전순 (`localeCompare`)
  3. **3순위 (낮은 가격순)**: 품목 단가가 저렴한 순서 (`lastPrice` 오름차순)
  - **엄격한 예산 제약**: 남은 예산 이하인 품목이 없을 경우, 예산 초과 품목을 제안하지 않고 추천 카드를 완전 숨김 처리한다.

- **Quick Select Chips (등록 품목 터치 선택) 정렬 순서**:
  1. **1순위 (낮은 가격순)**: 품목 단가가 낮은 순서대로 정렬 (`lastPrice` 오름차순)
  2. **2순위 (구매 빈도)**: 동일 가격대일 경우 구매 횟수가 많은 품목 (`useCount` 내림차순)
  3. **3순위 (가나다순)**: 한국어 사전순 (`localeCompare`)


## 29. 품목 입력 폼 라벨 및 초기화 비활성화 규칙 (Form Label & Reset Deactivation)

- 품목 입력 폼 상단의 라벨 문구(`품목 (선택)`, `가격 (필수)`)를 완전히 제거하여 깔끔하고 직관적인 미니멀 레이아웃을 구성한다.
- 저장 품목의 예기치 않은 전량 삭제를 방지하기 위해 `품목 초기화` 버튼 및 기능은 UI 화면에서 완전히 비활성화(제거) 처리한다.


## 30. 코스트코 품목 가격 100% 검증 규칙 (Costco Item Price Validation)

- `품목_코스트코.md` 및 코스트코 기본/저장 품목 목록에는 가격이 미입력되거나 비어있는 항목을 완전 제거한다.
- 단가가 명확히 기재된 유효 24종 품목 데이터만 엄격하게 관리 및 동기화한다.
- 코스트코 품목은 화면 표시 시 단가 끝 두 자리가 `70` 또는 `00`이면 품목명 앞에 `🔥`를 붙인다.
- `🔥` 표시는 UI 전용이며 실제 저장 품목명, 검색 키, DB 품목명에는 포함하지 않는다.
- 같은 품목의 접두어(`와인` 등), 띄어쓰기, 표기 차이(`샤도네이`/`샤르도네`)가 발견되면 대표명을 정하고 `품목_코스트코.md`, 기본 품목, Neon DB를 함께 병합한다.
- 신규 등록/업로드 시 대표 품명 예시는 `소노마 샤르도네`, `기린캔 500ML X 8`처럼 상품 식별에 필요한 핵심명과 규격을 포함한 이름을 우선한다.


## 31. 초성 및 단자음/쌍자음 호환 검색 규칙 (Strict Chosung Boundary & Tense Consonant Search)

- 품목 검색(자동완성 입력) 시 초성 입력(예: `ㄱ`, `ㄷ`)은 **품목명/단어의 시작 초성(Chosung) 경계 매칭**만 엄격히 수행하며, 음절 내부의 받침/종성(Jongsung)에 의한 잘못된 오매칭을 원천 차단한다.
  - `ㄱ` 입력 시 ➡️ `그린키위` (`ㄱ`), `깐대파` (`ㄲ`), `국산콩물` (`ㄱ`) 검색 성공 / `맥주` (`ㅁ` 시작, 받침이 `ㄱ`) 오매칭 방지
  - `ㄷ` 입력 시 ➡️ `딸기쨈` (`ㄸ`), `대파` (`ㄷ`) 검색 성공
- 단자음 입력으로도 대응하는 쌍자음을 검색할 수 있도록 문자 정규화(`normalizeTenseConsonants`)를 제공한다 (`ㄱ` ↔ `ㄲ`, `ㄷ` ↔ `ㄸ`, `ㅂ` ↔ `ㅃ`, `ㅅ` ↔ `ㅆ`, `ㅈ` ↔ `ㅉ`).
- 2자 이상 초성 검색은 단어 경계 이후의 연속 초성도 허용한다.
  - `방울 토마토`는 `ㅌㅁㅌ`로 검색 성공해야 한다.
  - 공백 없이 저장된 `방울토마토`도 `ㅌㅁㅌ`로 검색 성공해야 한다.
  - 한 글자 초성 검색은 기존 경계 매칭 원칙을 유지해 받침/종성 오매칭을 방지한다.


## 32. 단일 매칭 품목 엔터 입력 가격 검토 이동 규칙 (Single Match Enter Auto-Fill & Price Focus)

- 품목 입력란에 초성이나 텍스트(예: `ㄸㄱㅉ`, `ㄲㄷㅍ`)를 입력하여 자동완성 매칭 결과가 **단 1개의 품목(`currentMatches.length === 1`)으로 일치할 경우**:
  - 엔터(`Enter`) 키를 누르면 마우스 클릭이나 가격 재입력 없이 해당 1개 매칭 품목의 **품목명과 저장된 최근 단가가 입력란에 자동 세팅**되며, **가격 입력 상자로 포커스(`itemPriceInput.focus()`)가 즉시 이동**하여 가격을 검토 및 수정할 수 있게 지원한다.
  - 가격 검토 후 엔터 키를 누르면 최종 장바구니 담기가 완료된다.


## 33. 변경 이력 지속 관리 가이드 (Change History Maintenance Guideline)

- 코드를 수정하거나 새로운 기능을 추가/업데이트할 때마다 `history.md` 파일에 해당 버전과 변경 내역(신규 기능, 정밀 알고리즘 개선, UI/UX 변경 등)을 누적하여 체계적으로 기록한다.
- `history.md` 작성 시 기술 표준:
  1. **버전 및 수정 일자**: `vX.X (YY-MM-DD)`
  2. **변경 항목 상세**: 주요 개선점 및 해결된 이슈 요약
  3. **명세 동기화**: `dev.md` 해당 규칙 섹션과 즉시 상호 연동 기록
  4. **배포 내역**: 사용자가 배포를 요청해 GitHub `main`에 푸시한 경우 커밋 해시와 배포 요약을 함께 기록


## 34. 장바구니 품목 최신순 상단 추가 규칙 (Cart Items Top Insertion Order)

- 장바구니에 품목을 추가할 때(폼 입력 담기 또는 스마트 추천 담기), 신규 품목은 장바구니 리스트의 **최상단(`index 0` / `cart.unshift`)에 위치하여 최신순으로 위에서부터 누적**되도록 배치한다.
- 사용자는 가장 최근에 담은 품목을 스크롤 이동 없이 장바구니 맨 위에서 즉각 확인 및 조작할 수 있다.


## 35. 자동완성 추천 드롭다운 위치 격리 규칙 (Autocomplete Dropdown Positioning)

- 품목 입력 시 초성/텍스트 자동완성 드롭다운 레이어(`.autocomplete-list`)는 **품목 입력 상자(`#item-name-input`) 전용 컨테이너(`.autocomplete-container`) 바로 아래(`top: 100%`)**에 고정 표출된다.
- 아래쪽의 가격 입력 상자(`#item-price-input`)와 시각적으로 겹치거나 가리는 현상을 원천 차단하며, 등록 품목 터치 선택 칩 위에 깔끔한 레이어(`z-index: 50`) 형태로 펼쳐진다.


## 36. Neon Postgres DB 동기화 및 구매 내역 저장 규칙 (Neon Postgres Sync & Purchase Records)

- 현재 앱의 주 저장소는 `pg` 기반 Neon Postgres이며, `DATABASE_URL` 환경 변수를 통해 연결한다.
- localStorage는 브라우저 fallback/cache 역할로 사용한다.
- 서버는 `db.js`를 통해 마트, 품목, 가격 이력, 공유 장바구니, 예산, 구매 저장 내역을 관리한다.
- 주요 DB API:
  - `GET /api/db/sync?mart=<mart>`: 예산, 등록 품목, 가격 이력, 장바구니를 한 번에 조회한다.
  - `POST /api/db/item`: 등록 품목 단가를 추가/수정하고 가격 이력을 남긴다.
  - `DELETE /api/db/item?mart=<mart>&name=<name>`: 등록 품목을 DB와 마크다운 백업에서 삭제한다.
  - `POST /api/db/cart`: 현재 장바구니 상태를 저장한다.
  - `POST /api/db/budget`: 마트별 예산을 저장한다.
  - `POST /api/save-record`: 계산결과를 DB 구매 내역에 저장한다.
  - `GET /api/db/records?mart=<mart>`: 저장된 구매 계산 내역을 조회한다.
  - `DELETE /api/db/records/:id`: 개별 저장 내역을 삭제한다.
- `POST /api/save-record`에서 DB 저장 실패가 발생하면 조용히 무시하지 않고 서버 오류로 반환해야 한다.
- Vercel 서버리스 환경에서는 로컬 Markdown 백업 파일의 영구 저장을 보장하지 않으므로, 구매 내역의 기준 저장소는 Neon Postgres로 본다.
- 계산결과 저장 성공 시 저장 내역 모달이 열려 있으면 즉시 목록을 새로고침한다.


## 37. 등록 품목 & 단가 목록 모달 규칙 (Registered Items Modal)

- `등록 품목/가격 조회` 버튼은 `#all-items-modal`을 열어 현재 마트의 등록 품목과 단가를 낮은 가격순으로 표시한다.
- 목록 행 구성:
  - 좌측: 품목명
  - 우측: 단가 직접 입력칸(`.all-item-price-input`), 가격 이력 버튼(`.btn-item-history-modal`), 삭제 버튼(`.btn-item-delete-modal`)
- 단가 수정은 별도 `prompt()` 팝업을 사용하지 않는다.
- 사용자는 단가 입력칸에서 숫자를 바로 수정하고 `Enter`를 누르거나 입력칸 밖으로 포커스를 이동해 저장한다.
- 단가 저장 중에는 입력칸을 비활성화하고 `.saving` 상태를 적용한다.
- 저장 성공 후 입력값은 천 단위 콤마 형식으로 정리한다.
- 저장 실패 시 이전 단가로 되돌리고 사용자에게 오류 토스트를 표시한다.
- 단가 변경은 `POST /api/db/item`으로 서버 DB와 동기화하며, `incrementUse`는 `false`로 보낸다.


## 38. 가격 이력 모달 표시 규칙 (Price History Modal Layering)

- 등록 품목 목록, 장바구니 품목, 자동완성 항목, 빠른 선택 칩에서 가격 이력 버튼을 누르면 `#price-history-modal`을 연다.
- 등록 품목 목록에서 가격 이력 버튼은 단가 입력칸의 `blur/change` 저장 이벤트와 충돌하지 않도록 `pointerdown`에서도 즉시 처리한다.
- 가격 이력 모달을 열 때는 `document.body.appendChild(historyModal)`로 DOM 맨 뒤로 이동시켜 다른 모달보다 나중에 그려지도록 한다.
- 가격 이력 모달은 `.modal-topmost`를 적용하고 `z-index: 2147483000 !important`로 최상단 레이어를 보장한다.
- 모달 표시 시 `display: flex`, `visibility: visible`, `opacity: 1`, `pointer-events: auto`를 명시해 숨김 상태가 잔존하지 않도록 한다.
- 가격 이력 닫기 버튼을 누르면 `.modal-topmost`와 inline style을 제거하고 `.hidden`을 다시 적용한다.


## 39. 전광판 버전/날짜 배지 규칙 (Dashboard Version Badge)

- 전광판의 버전/날짜 정보는 `#app-version-badge`로 관리한다.
- 실제 HTML 클래스는 `.version-badge`이며, CSS는 `.version-badge`와 `.app-version-badge` 모두에 동일 위치 스타일을 적용한다.
- 배지는 전광판 오른쪽 위(`top: 10px; right: 14px`)에 absolute 배치하며 줄바꿈 없이 표시한다.
- 앱 코드 또는 기능을 수정할 때 `public/app.js`의 `APP_VERSION`, `public/index.html`의 배지 텍스트, `package.json`, `package-lock.json`, `history.md`를 같은 버전으로 동기화한다.
- 전광판 오른쪽 아래에는 `.db-badge.neon-postgres-badge`로 Neon DB 로고만 고정 배치한다.


## 40. 최신 기준 v1.3.82 보강 사항 (Current v1.3.82 Notes)

- 현재 기준 버전은 `v1.3.82 (26-08-08)`이다.
- `v1.3.1` 이후 주요 보강:
  - 등록 품목 삭제 API(`DELETE /api/db/item`) 추가.
  - 장바구니 DB 저장 내역 모달 닫기 동작 복구.
  - 계산결과 저장 피드백 및 DB 실패 처리 강화.
  - 등록 품목 가격 이력 버튼 열림 문제 보강.
  - 등록 품목 단가 입력을 별도 팝업에서 직접 입력 방식으로 전환.
  - 단가 입력 저장 트리거(`blur/change/Enter`) 안정화.
  - 가격 이력 모달 최상단 표시 강제.
  - 전광판 버전/날짜 배지 우측 상단 배치 보정.
  - Neon Postgres 저장소 전환 및 `NeonDB` 전광판 배지 추가.
  - 전광판 오른쪽 아래 저장소 표시를 Neon Postgres 로고형 배지로 개선.
  - 로컬 `npm run dev` 실행 시 `.env.local` 또는 `.env`의 `DATABASE_URL`을 먼저 로드해 Neon Postgres 연결 오류를 방지.
  - 전광판 Neon Postgres 배지에 실제 Neon 로고 이미지(`public/assets/neon-db-logo.png`)를 적용.
  - 전광판 저장소 배지에서 `Postgres` 텍스트를 제거하고 Neon DB 로고만 표시.
  - Neon DB 로고 PNG의 검은 배경을 투명 처리하고 배지 배경/테두리를 제거.
  - 화면 버전 날짜 표기를 네 자리 연도에서 두 자리 연도(`26-08-08`)로 축약.
  - 코스트코 품목 단가 목록에 캠벨포도, 설빙 미숫가루, 기린캔, 홈스타욕실클리너 등 14개 요청 단가를 반영.
  - 천 단위 쉼표가 있는 단가를 잘못 분리하지 않도록 첫 쉼표 뒤 전체를 단가 문자열로 읽는 파싱 규칙을 명시.
  - 코스트코 단가 끝 두 자리가 `70` 또는 `00`인 품목은 화면 품목명 앞에 `*`를 표시.
  - 등록 품목 모달에서 단가를 수정해 별표 조건이 바뀌면 빠른 선택칩과 자동완성 표시도 즉시 갱신.
  - 코스트코 `70원/00원` 표시 아이콘을 `*`에서 `🔥`로 변경.
  - `와인 소노마 샤르도네`와 `소노마 샤도네이`를 `소노마 샤르도네`로 병합하고 기본 코스트코 목록을 현재 가격표 기준으로 정리.
  - `기린캔 8개`와 `기린캔 500ML X 8`을 대표명 `기린캔 500ML X 8`로 병합.
  - 품목 신규 등록/업로드 전 중복 검토 후 동일 품목은 대표 품명으로 단가만 갱신하도록 가이드 추가.
  - 품목 파일 업로드 및 업로드 후 `.md` 생성 시점에도 대표 품명 규칙을 적용.
  - 품목/가격 정보 다운로드 헤더의 파일명을 ASCII 안전 이름으로 생성해 한글 마트명으로 인한 `Content-Disposition` 오류를 방지.
  - 품목/가격 정보 다운로드에 가격 변동 이력 기반 최저/최고/평균 컬럼을 추가.
  - 영수증 원문을 구매내역 DB 저장용 JSON으로 변환하는 `receipt.md` 가이드를 추가.
  - 영수증 변환 결과를 `영수증_YYYY-MM-DD.json` 파일명으로 저장하도록 `receipt.md` 가이드를 보강.
  - 영수증 변환 시 구매일시를 필수로 포함하고, 없으면 사용자에게 먼저 입력받도록 `receipt.md` 가이드를 보강.
  - 영수증 변환 결과에 `martName`을 필수로 넣고, 마트별 품명 중복 후보와 정리 이력을 `품명_중복정리_이력.md`로 분리.
  - 영수증 JSON 업로드 시 `martName`이 `코스트코` 또는 `이마트`가 아니면 저장하지 않도록 검증.
  - 구매내역 저장 시 한글 `코스트코` 마트명을 DB 표준 키 `Costco`로 변환하도록 서버 매핑을 보강.
  - 영수증 변환 시 쿠폰 할인은 대상 품목 금액에서 차감하고, 동일 품목은 수량/최종 금액 합산 후 JSON을 만들도록 `receipt.md` 가이드를 보강.
  - 저장내역 조회 모달에 월별 조회 필터를 추가하고, 조회/업로드/다운로드 버튼 4개를 2 x 2 배열로 정리.
  - 저장내역 조회 API에서 한글 `코스트코`를 DB 표준 키 `Costco`로 변환해 월별 조회 누락을 방지.
  - 기존 저장내역의 한글 마트 키(`코스트코`, `이마트`)를 표준 키로 정리하고, 조회 시 표준/한글 키를 함께 포함하도록 보강.
  - 영수증 변환 JSON이 DB 저장에 바로 들어가도록 품목명 정제 순서, 필드 검증, 합계 검산, 할인 반영 규칙을 `receipt.md`와 `품명_중복정리_이력.md`에 보강.
  - 영수증 JSON 업로드 시 계산 합계와 `totalAmount`가 일치하지 않으면 저장하지 않도록 검증.
  - 저장내역 조회 화면을 월별 접기/펼치기 섹션으로 변경해 긴 내역을 한 화면에서 쉽게 탐색하도록 개선.
  - 저장내역 조회 기본 월을 현재 년월로 설정하고, ▲/▼ 버튼으로 다음월/이전월 이동 조회가 가능하도록 개선.
  - 저장내역 조회에서 `조회`는 선택 월의 현재 마트 내역, `전체`는 선택 월의 전체 마트 구매내역을 조회하도록 동작을 구분.
  - 저장내역 조회에 `연간` 버튼을 추가해 선택한 연도의 현재 마트 전체 구매내역을 조회하도록 개선.
  - 연간 조회 화면에 현재 마트의 연간 구매 총합 배너를 표시.
  - 연간 조회 캡션에서 불필요한 선택 월 표시를 제거.
  - 저장내역의 `조회` 버튼을 현재 월 현재 마트 구매내역 조회로 고정.
  - 저장내역의 조회/연간/전체 모든 모드에 구매 총합 배너를 표시.
  - 저장내역 카드에 구매한 마트 배지를 표시해 전체 조회에서 마트 구분이 가능하도록 개선.
  - 코스트코 품목 정규화에서 `KS새우` 접두어를 제거하고 규격별 `새우 ...` 대표 품명으로 통합.
  - 화면 문구 `저장내역조회`를 `구매내역 조회`로 변경하고 관련 모달/안내 문구를 정리.
  - `계산결과 저장 (DB 저장)` 버튼 문구를 `장바구니 저장`으로 변경.
  - 영수증 JSON 변환 원칙에 공백/하이픈/용량 표기 차이 품목의 대표 품명 표준화 규칙을 보강.
  - 구매내역 조회 카드의 품목 표시를 `품목 / 개수 / 총합` 3열 구조로 변경.
  - 구매내역 삭제 시 구매일시와 금액을 포함한 확인 절차를 표시하고 취소 안내를 추가.
  - 구매내역 카드의 품목 목록을 가나다순으로 표시.
  - 구매내역 조회 카드에서도 코스트코 특이 가격 품목에 `🔥` 표시가 나오도록 보강.
  - 영수증 JSON 업로드 시 DB 저장 전 미리보기 모달에서 품목/수량/총합을 확인한 뒤 저장하도록 개선.
  - 구매내역 조회에 `🏆 랭킹` 버튼을 추가해 선택 월의 품목 소비 TOP 10을 수량순/금액순으로 확인하도록 개선.
  - `로컬 파일로 저장 (MD 저장)` 버튼을 장바구니 액션 영역 맨 아래로 이동.
  - 장바구니 영역의 `로컬 파일로 저장 (MD 저장)` 버튼을 제거하고, 구매내역 조회 모달에 현재 조회 결과 Markdown 저장 버튼을 추가.
  - 랭킹 조회를 `월간 랭킹`, `연간 랭킹`으로 분리.
  - 구매내역 조회 필터 버튼을 2줄 3열로 정렬하고 랭킹 버튼에 아이콘을 표시.
  - 구매내역 조회의 `조회/연간/전체` 버튼을 월 선택 줄에 함께 배치.
  - 구매내역 조회 월 선택 입력의 달력 팝업을 제거하고, `▼/▲` 버튼으로만 월을 조정하도록 변경.
  - 구매내역 조회의 월 선택/조회/연간/전체 줄 버튼 폭을 압축해 한 줄 배치를 유지하도록 조정.
  - 구매내역 조회 월 표시 폭과 상단 필터 간격을 추가 축소.
  - 해당 월의 둘째·넷째 일요일 날짜를 계산해 이마트 탭 전광판에 `0월 0일, 0일 이마트 휴무일` 형식으로 표시.
  - 코스트코 탭에서도 해당 월 둘째·넷째 일요일 날짜를 전광판에 표시하고, 휴무 당일에는 탭을 `코스트코(휴무)`로 표시.
  - 이마트 탭은 해당 월 둘째·넷째 일요일 당일에만 `이마트 (하남)`으로 표시.
  - 전광판 휴무일 안내에서 월/일 사이 `/` 구분자를 제거.
  - 휴무일 당일에는 전광판 휴무일 안내 문구가 점멸되도록 표시.
  - 전광판 휴무일 안내에 영업시간을 함께 표시한다. 이마트 하남점은 `10:00~23:00`, 코스트코 하남점은 `10:00~22:00` 기준이다.
  - 전광판과 품목 초기화 버튼에서는 내부 키 `Emart` 대신 화면 표시명 `이마트`를 사용한다.
  - 이마트 기본 예산은 평일/토요일 6만원, 일요일 5만원으로 분리한다.
  - 일요일 이마트 기본 예산 5만원은 Neon DB `budgets`에도 저장되도록 `/api/db/sync`와 `/api/db/budget` 동기화를 보강한다.
  - Neon DB `app_settings`에 기본 예산 설정값(`emart_regular_default_budget`, `emart_sunday_default_budget`, `costco_default_budget`)을 저장한다.
  - 영수증 JSON 업로드 예제 파일은 `예제_영수증_Costco.json`, `예제_영수증_Emart.json`을 참고한다.
  - 품목 MD 업로드 예제 파일은 `예제_품목_Costco.md`, `예제_품목_Emart.md`를 참고한다.
  - `📒 가계부` 버튼을 추가하고 `/api/db/household-summary?month=YYYY-MM` API로 월별 가계부를 조회한다.
  - 가계부 예산은 이마트 `290,000원`, 코스트코 `300,000원`, 전체 `590,000원` 기준으로 계산한다.
  - 가계부에는 잔여/초과, 지난달 대비 증가/감소, 연간 누적 지출, 마트별 비중, 예산 80% 이상 경고를 표시한다.
  - 사용자가 영수증 JSON 변환을 요청하면 JSON 문법과 합계를 검증하고, 통과한 경우 `영수증_YYYY-MM-DD.json`으로 저장한다.
  - 영수증 JSON 검증에 실패하면 파일 저장을 하지 않고 오류 사유를 먼저 보고한다.
  - `영수증_YYYY-MM-DD.json` 업로드 버튼을 추가하고 JSON의 `timeStr/items/totalAmount`를 구매내역 DB에 저장.
  - `방울 토마토`/`방울토마토`를 `ㅌㅁㅌ`로 찾을 수 있도록 2자 이상 연속 초성 검색 보강.
  - 개발 이력과 절차 문서를 최신 배포 및 DB 기준으로 동기화.

